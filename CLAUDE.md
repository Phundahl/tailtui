# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A lightweight, keyboard-driven terminal UI — **tailTUI** (visual codename "Matrix Core") — that wraps Tailscale, built on the Charmbracelet stack. `_designs/00_WIREFRAME_SPEC.md` (layout + keybinding matrix) and `_designs/00_STYLE_GUIDE.md` (palette) are the authoritative design sources. (An earlier `design-spec.md` was removed; anything still citing it means these.) The project is built **iteratively in numbered phases**, each ending with user approval before the next begins.

Branding: the product name is **tailTUI** (shown in the header; version in the footer via `appName`/`appVersion` in `view.go`). The Go module path is `github.com/Phundahl/tailtui` and the theme-override env var is `TAILTUI_THEME` (both renamed in Phase 16). The repo directory is `tailtui`.

**Attribution / pseudonym rule (Phase 26.2, standing).** The only attribution form anywhere in this repo is **`Phundahl`** (the GitHub handle). No first names, full names, or other real-name fragments may appear in source-file headers, code comments, docstrings, markdown docs, the phase log, commit messages, or PR descriptions — including when *describing* a fix that anonymized one (paraphrase rather than quoting the prior value). The GitHub-username import path `github.com/Phundahl/tailtui` is the only allowed occurrence of the handle itself; the LICENSE copyright holder, README acknowledgments / license footer, and any future attribution must read `Phundahl`.

## Commands

```bash
go run .              # run the TUI (needs a real terminal / TTY)
go build ./...        # compile everything
go vet ./...          # static checks
go test ./...         # tests (View-layer smoke tests in internal/tui/*_test.go)
go test ./internal/tui/ -run TestName -v   # run a single test
```

The View layer is testable without a TTY: construct `tui.New()`, send a `tea.WindowSizeMsg`, then call `Update`/`View` directly (see how the phase smoke tests were written). Prefer this over launching the program when verifying render/logic changes.

## Standing constraints (do not violate without being asked)

- **Live node data + live latency via the `tailscale` CLI (Phases 6–7).** Node list, details, local node, advertised routes, and the latency graph all come from the real CLI through `internal/tailscale` — mapped into the existing `internal/types` structs (never replacing them). Status from `tailscale status --json`; latency from `tailscale ping`. The **accounts** modal is live too (Phase 14): `tailscale switch --list --json`. The **Advanced Settings** modal is live too (Phase 20): local prefs read from `tailscale debug prefs` and toggled via `tailscale set --<flag>=<bool>`. The **Routing Management** modal (Phases 21–23) reads the advertised exit-node + subnet routes from the same `tailscale debug prefs` (`AdvertiseRoutes`; the exit defaults `0.0.0.0/0`+`::/0` are split out into `Prefs.AdvertiseExitNode`), lets the user **stage edits locally** (toggle exit-node, add/remove routes), and applies them via `tailscale set --advertise-exit-node/--advertise-routes` **only through the "Command Room" confirmation** (`[Enter]`), which previews the exact command and offers a clipboard copy. The **SSH launcher** is live too (Phase 34): per-peer capability is read from `sshHostKeys` in the same `tailscale status --json`, the local operator from `tailscale debug prefs` (`OperatorUser`), and `[s]` execs the real `tailscale ssh`. There is **no mock data anywhere** in the app — Phase 18 deleted the `internal/mock` package entirely: the TERMINAL_LOGS ring now **starts empty** and fills only with real runtime events, and node/latency/account data all come live from the CLI. Don't reintroduce mock data.

- **Log system (FIFO ring + `[v]` overlay, Phase 8).** `Model.logs []types.LogEntry` is the single event log; `Model.appendLog(level, message)` timestamps (`15:04:05`) and appends, enforcing a **FIFO cap of `maxLogEntries = 500`** (oldest dropped) so a long session can't leak memory. Real events are logged: exit-node action success/failure (`actionMsg`, the actual `tailscale set` stderr lands as `[ERROR]`), and status connectivity transitions (logged once on fail→ok / ok→fail, never per-tick, so a down daemon doesn't flood the ring). The bottom **TERMINAL_LOGS pane is a fixed-height tail** — `renderLogs` shows only the last 2 entries (plus the transient `fetchErr` line) so UI ticks can't bury a message. **Log lines are syntax-highlighted** (Phase 11.5) via `styles.LogLevelColor(level)` — ERROR→`Danger` (red), INFO→`Primary` (green), WARN→`Warn` (yellow), DEBUG→`Secondary`, else→`Subtle` — applied to the level chip only; the timestamp is dimmed (`Subtle`/`ModalDim`) and the message stays default text. Both renderers share the helper: `logTailLine` (over Background) and `logLine` (over Surface + bold, matching the modal status chips). `[v]` opens the full-history **log overlay** (`stateLogs`): a scrollable viewport in an opaque, Surface-filled modal framed by the **pane-style titled border** `┌─┤ TERMINAL_LOGS ├──┐` (`renderLogOverlay` + `modalTitledTop` replicate `styles.Pane`'s border opaquely, rather than the `⌐ TITLE ¬` tab the other modals use). It uses its own **`logOverlayWidth`** (~85% of the terminal, capped at 120 cols) — much wider than the standard `overlayWidth` — so timestamped log lines with network paths fit on one line instead of wrapping. `[v]`/`esc`/`q` close it; `j/k` scroll.

- **Connection toggle (`c`, `tea.ExecProcess`).** The LOCAL_NODE pane's button is **state-aware** (`renderConnectButton` / `m.localConnected()`, derived from `m.local.Conn != ConnOffline`): green `[c] Connect` when the node is down, warning-yellow `[c] Disconnect` when it's up. `[c]` runs `connectCmd(!localConnected())` → `tailscale up` (down→up) or `tailscale down` (up→down) via `tea.ExecProcess` so an auth URL printed by `up` is visible and interactive — **no sudo** (the user is already a configured operator). `connectDoneMsg` logs the outcome and fires `fetchStatusCmd` so the new state shows immediately. The footer mirrors this: a dynamic `[c] Connect`/`[c] Disconnect` hint and a `● CONNECTED`/`○ DISCONNECTED` status that track `m.local.Conn`.

- **Operator setup (`O`, `tea.ExecProcess`).** Tailscale's `checkprefs access denied` happens when the user isn't the tailscaled operator. `[O]` (shift+o) runs `operatorSetupCmd`: a `tea.ExecProcess` that **suspends the TUI, releases the terminal**, and runs interactive `sudo tailscale set --operator=$USER` (`currentUser()` prefers `$USER`, falls back to `os/user`). stdin/stdout/stderr are left unset so ExecProcess wires them to the program's terminal (the sudo password prompt works); the TUI is restored automatically on exit and an `operatorDoneMsg` logs the result and triggers an immediate `fetchStatusCmd` so the new permissions take effect. This is the one place we hand the real terminal to a child process.

- **Exit Latency (LOCAL_NODE pane).** The local node has no self-RTT, so its dashboard latency field is relabeled **"Exit Latency:"** and shows the live ping to the *active exit node* (the node all traffic routes through): `renderExitLatency` reads `Model.latency[activeExitNodeIP()]`. To keep it live even when the exit node isn't highlighted, the ping ticker pings **both** the selected node **and** the active exit node each tick. Shows `N/A` when no exit node is active (or it's offline), `—` until the first sample arrives.
- **Exit State (LOCAL_NODE pane, Phase 17).** The LOCAL_NODE field formerly labeled **"State:"** (which showed the *local* node's own connectivity) is renamed **"Exit State:"** and is now strictly tied to the exit-node feature: `renderExitState` scans the peer list (the source of truth) for the `IsActiveExitNode` peer and renders **that peer's** route status — `connSymbol` + `connText` → `DIRECT`/`RELAY (via …)` — exactly as it comes from `tailscale status --json`. When no exit node is active it shows a dim **`N/A`**, keeping the LOCAL_NODE layout static and consistent with the sibling `Exit:` / `Exit Latency:` inactive readouts. The local node's *own* `Conn` still drives the `[c]` connect button and the footer status, just not this field.

- **LOCAL_NODE action buttons & flush symmetry (Phases 20–21.1).** The LOCAL_NODE pane stacks the state-aware `[c]` connect button over a single grouped action row `[S] Advanced Settings    [R] Routing` (`renderActionButtons`). The pane height is `localNodeH = 11` and **PEER DETAILS is locked to it (`detailsH = localH`)** — so any change to the LOCAL_NODE content height must keep the two heights equal (the horizontal grid line under the top panes must stay unbroken). `[S]`/`[R]` are **uppercase** (shift). Lowercase **`s` is the SSH launcher** (Phase 34); lowercase **`r` remains an explicit no-op**, reserved for a future routing action — don't bind it without freeing it deliberately.

- **Advanced Settings modal (`S`) — optimistic + reconcile.** Toggling a preference (`Space`) flips the local `Model.prefs` field **immediately** for instant feedback **and** fires `setPrefCmd` → `tailscale.SetPref` (`tailscale set --<flag>=<bool>`) off the UI thread; the resulting `prefActionMsg` logs the outcome and **re-fetches prefs** (`fetchPrefsCmd`) so the checkbox reconciles with the daemon's true state, reverting a failed flip. Never block the Update goroutine on the CLI; never hardcode the flag list outside `settingDefs`.

- **Routing Management modal (`R`) — local working copy, applied only via the Command Room.** Edits operate on a **working copy** (`Model.routingExitNode` / `routingRoutes`) snapshotted from `prefs` at open; they **must never mutate `m.prefs`** (the daemon's last-known truth). Added routes are **validated with `net.ParseCIDR`** before entering the list; invalid input is rejected (red-flashed, cleared) without crashing. A `[d]`-deleted route is stashed in `lastDeletedRoute` and **pre-filled into the next `[a]`** (a lightweight pseudo-undo / edit-typo affordance) — the pre-fill flows through the same `net.ParseCIDR` validation, so it is not a bypass. The CIDR editor is a `bubbles/textinput` (`routingInput`), styled to blend into the modal **Surface**. Two things keep it from rendering a black block (the "black box" glitch): (1) **no in-field `Placeholder`** — a placeholder makes the empty field render via `placeholderView`, which fills the remaining Width with **raw, unstyled (near-black) spaces** that no outer Surface wrap can recolor (they sit mid-line after a reset); with no placeholder the main path pads with `TextStyle` (Surface bg) instead, so every cell is Surface (the prompt label already shows the "(e.g., 192.168.1.0/24)" example). (2) the cursor is a **visible bright Primary block**: `bubbles/cursor` draws its visible cell with `Style.Reverse(true)`, which swaps fg/bg at display time, so the **displayed background is `Style`'s Foreground** — set `Cursor.Style` = `Foreground(Primary).Background(Bg)` so the reverse yields a solid Primary block (dark glyph), neither invisible (the 23.2 `Foreground(Surface)` camouflage, which blended into the modal) nor a black block. `Cursor.TextStyle = ModalText` keeps the blink-"off" phase as normal text on Surface. (`PromptStyle`/`TextStyle` also paint Surface.) While in input mode an early guard in `updateOverlay` routes **every** key (including `esc`/`q`) to `updateRoutingInput`, so the global close handler can't fire mid-entry (`Esc` cancels the entry; `Enter` confirms). `routingDirty` prevents a late `prefsMsg` from clobbering staged edits; `refreshRoutingOverlay` re-renders **and** resizes the viewport when switching sub-modes so the editor is never clipped.

- **Routing execution — the "Command Room" (`stateRoutingConfirm`, Phase 23).** The **only** place routing changes reach the daemon. `Enter` in the routing list opens a floating confirmation overlay (`renderRoutingConfirmOverlay`, rendered directly from model state like the settings modal) showing the **exact command** to be run — assembled by `tailscale.AdvertiseCommandString(exitNode, routes)` and identical to what executes — plus the **Admin Console approval reminder**. The command **always sets both flags** (`--advertise-exit-node=<bool>` and `--advertise-routes=<csv>`); an empty routes list renders `--advertise-routes=` (the idiomatic "clear", never omit). Long previews wrap safely (`wrapCommand` = word-wrap + hard-wrap fallback for the comma-joined CIDR token). Keys: **`Enter`** applies — fires `setRoutingCmd` → `tailscale.SetRouting` **off the UI thread**, closes the modal to `stateMain`, and the `routingActionMsg` logs the executed command + batches `fetchStatusCmd`+`fetchPrefsCmd`; **`c`/`C`** copies the command to the system clipboard via `copyRoutingCmd` (async, **`github.com/atotto/clipboard`** — shells out to pbcopy/wl-copy/xclip/clip, so a missing tool logs an error instead of crashing) and flashes a `✓ Copied!` indicator (`routingCopied`, set by `clipboardMsg`), staying in the modal; **`Esc`/`q`** go **back to the routing list** (not all the way out) without applying. `updateOverlay` routes `stateRoutingConfirm` keys to `updateRoutingConfirm` *before* the global esc/q close handler so Esc means "back," not "quit the feature."
- **SSH launcher (`s`, Phase 34) — preview, then hand over the terminal.** `[s]` on the highlighted peer opens `stateSSH` (`internal/tui/ssh.go`), which previews and then runs `tailscale ssh [user@]host` via `tea.ExecProcess`. Invariants:
  - **Capability is advisory.** `Peer.OffersSSH` comes from the status JSON's per-peer `sshHostKeys` (present only when that node runs the Tailscale SSH server). It proves the *server* exists, **never** that the ACL lets you in — per-peer `CapMap` is always `null`, so a marker plus a refused connection is an expected, unfixable outcome. The modal's dim `* Requires … an ACL rule` note is the only mitigation, and is the structural twin of the Command Room's Admin Console reminder.
  - **Gating is looser than `x` on purpose.** `[s]` is offered on **any online peer**, not only SSH-advertising ones, because a peer without Tailscale SSH may still run a plain sshd. Offline peers and an empty selection are silent no-ops. Don't tighten this to `OffersSSH` — it would make the feature invisible on a tailnet where nobody advertises it.
  - **Two sub-modes, and it is forced.** An always-focused username field and a `[c]` copy key are mutually exclusive (`c` cannot both type a "c" and copy). So the modal opens in **nav mode** with the field *blurred* and pre-filled; **`[e]`** enters the editor; `Enter`/`Esc` leave it. This mirrors the routing list/input split exactly — keep it that way rather than inventing a second pattern. `ctrl+c` is unavailable as a copy key (`Update` intercepts it as quit before overlay routing).
  - **Preview == execution, structurally.** `tailscale.SSHCommandString` is built *from* `SSHArgs`, the same argv `sshLaunchCmd` runs, exactly as `AdvertiseCommandString` is built from `AdvertiseArgs`. Never let the two drift into independently-maintained strings. These helpers are **pure** and deliberately do **not** short-circuit on `mockEnabled` (neither do the Advertise pair — the blanket "every exported adapter func short-circuits first" claim is not true of the pure assembly helpers).
  - **Username**: `currentUser()` (now delegating to `tailscale.CurrentUser`, shared with the mock fixtures), then **session-sticky** via `Model.sshLastUser`, written only on a *confirmed* edit. In-memory only — there is no config file, and adding one for a single string would be disproportionate. `validSSHUser` rejects whitespace/`@`/`/` for **correctness** (it would split the argv token), *not* as a security boundary: no shell is involved anywhere in this path.
  - **Execution**: no sudo, and — uniquely in `poll.go` — **no `context.WithTimeout`**, because an interactive session is unbounded and a context would kill it mid-work. `exec.LookPath("ssh")` is pre-flighted so a missing system client lands in the log ring instead of flashing past during the restore. Mock mode never executes (VHS must not spawn a real ssh). The modal closes to `stateMain` **before** dispatch (the Phase 24.1 lesson).
  - **A non-zero exit is not an error.** `ssh` returns the *remote* command's status, so `exit 1` in your own shell would otherwise paint a red ERROR. `sshDoneMsg` classifies `*exec.ExitError` as **WARN with the code** and reserves ERROR for a failure to start.
  - **Height is a live constraint.** `overlayCenter` silently drops foreground lines past the background height, so an over-tall modal loses its *bottom* — the keymap — without ever breaking the flush invariant. `TestSSHOverlayFitsMinimumTerminal` (72×24) guards it; keep the keymap in two `gridLine` columns if the body grows.

- **Shared modal editors (`newModalInput`, `overlay.go`).** Every modal text field — the routing CIDR editor and the SSH username — is built through `newModalInput(charLimit)`, the single home for two hard-won rendering fixes: **never set `Placeholder`** (its empty-state path emits raw near-black padding) and the cursor's `Style` must be `Foreground(Primary).Background(Bg)` (bubbles/cursor draws the visible cell with `Reverse(true)`, so the *displayed* background is the Foreground). Those styles are captured **by value** at construction, so `restyleModalInput` re-applies them from `resizeOverlay` — the existing post-theme-reload hook — or a live theme switch leaves a stale cursor color baked into a stored `textinput`. Add a new editor only via this constructor, and add it to `modalInputs()` in `overlay_chrome_test.go` so it inherits the whole styling contract.

- **`clipboardMsg` carries a `kind`.** `clipboardRouting` is the **zero value** deliberately, so existing constructions stay valid. The kind travels on the message rather than being inferred from `m.state` at delivery time, which also closes a real race: press `c`, then `Esc` before the clipboard goroutine returns, and a state-based branch would flash the wrong indicator.

- **`[O] Operator` is a transient footer hint.** It renders only while `prefs.OperatorUser != currentUser()` — operator setup is a one-time step, and retiring the hint is what buys footer room for `[s] SSH`. The failure mode is correct by construction: if the prefs read fails *because* we aren't the operator, `m.prefs` is zero, the comparison fails, and the hint shows. The footer's left segment is clipped by `styles.Bar` (never wrapped), so an overflow is **silent** — `assertFlush` won't catch it. `TestFooterKeepsAllHintsAt120` asserts the last left-hand hint survives in both operator states.

- **Theme Engine (no more strict-ANSI rule).** Colors are TrueColor hex, centralized in a `styles.Theme` struct (`internal/styles/theme.go`). `DefaultTheme()` is the **"Matrix Core"** master design — the EXACT hex codes from the style guide's YAML frontmatter (`_designs/00_STYLE_GUIDE.md`): `primary #6bfb9a`, `background #0e150f`, `surface-container #1a211b` (Surface), `surface-bright #333b34`, `outline-variant #3d4a3e` (borders), `on-surface #dde5da` (text), `outline #869486` (dim), `tertiary #ffdd75` (warning), `error #ffb4ab`. `LoadTheme()` reads the **native Omarchy** theme, parsed with `github.com/pelletier/go-toml/v2`. It handles **two schema generations and two locations** (Phase 27): `themeCandidates()` probes `~/.local/state/omarchy/current/theme/colors.toml` (**Omarchy 4 "Quattro"** — it moved the current-theme symlink out of `~/.config`) then `~/.config/omarchy/current/theme/colors.toml` (**Omarchy ≤ 3**); `TAILTUI_THEME` short-circuits to a single candidate. `loadThemeFile` picks the mapping by **which keys are present** (`hasMarkers()` on each wire struct), never by filename — so a `TAILTUI_THEME` override of either vintage works, and `ThemePath()` reports the file that will actually be read. Mapping is per-field, so any missing key keeps its default; an unreadable/malformed/unrecognized file falls through to the next candidate and finally to `DefaultTheme()` — never crashes or leaves blanks. **v4 (semantic) → Theme**: `accent`→PrimaryAccent, `green`→SecondaryAccent, `background`→Background, `lighter_background`→Surface **in dark mode** / `dark_background`→Surface **in light mode**, `selection`→SurfaceBright, `muted`→BorderInactive+TextDim, `foreground`→TextNormal, `yellow` (falling back to `orange`, which themes like `white` need)→Warning, `red`→Error. **Legacy (flat palette) → Theme**: `accent`→PrimaryAccent, `color2`→SecondaryAccent, `background`→Background, `color0`→Surface, `color8`→SurfaceBright/BorderInactive/TextDim, `foreground`→TextNormal, `color3`→Warning, `color1`→Error. A file carrying **only** the three keys both schemas share (`accent`/`foreground`/`background`) is ambiguous-but-usable: those three apply, the rest default. **`Theme.Mode`** (`ModeDark`/`ModeLight`, from the v4 `mode` key; legacy and the default are always dark) exists solely to flip the **surface-ladder direction** — the elevated surface must move *away* from the canvas (lighter than a dark background, shaded against a light one), so taking `lighter_background` for a light theme is the bug the mode key prevents. Don't add a second consumer of `Mode` without re-checking that invariant. **tailTUI's own schema (Phase 30) is tried first** and outranks both Omarchy schemas: `tailtuiTheme` has one key per `Theme` field (`mode`, `primary`, `secondary`, `background`, `surface`, `surface_bright`, `border`, `text`, `text_dim`, `warning`, `error`) whose names are deliberately **disjoint** from the Omarchy vocabularies (only `mode`/`background` are shared, and both are excluded from `hasMarkers()`), so three-way detection stays purely a question of which keys appear. Within each theme directory `tailtui.toml` is probed **before** `colors.toml` — it is opt-in, so it is the more deliberate statement. It is what `contrib/tailtui.toml.tpl` renders to when installed into `~/.config/omarchy/themed/`, and it is equally hand-writable on any distro; **the reason it exists is to hand irreducible mapping calls back to the user** (e.g. `osaka-jade` defines `yellow` as a green, so no loader heuristic can pick a Warning color that is right for every theme). **Live reload (Phase 30)**: `styles.ThemeStamp()` returns the active file's path+mtime; `checkThemeCmd` (batched onto the existing `refreshInterval` tick, so no watcher dependency) re-stats it off the UI thread and re-parses on change, returning a `themeMsg`. **The parsed `Theme` is returned, never applied, inside that closure** — `styles.Apply` rewrites package-level vars that `View` reads, so it must run in `Update` on the single Elm goroutine or it races the renderer. The `themeMsg` handler applies the palette, records the new stamp (**even when unchanged**, so a theme file appearing or vanishing is noticed), logs once, and calls `resizeOverlay()` when an overlay is open — viewport-backed overlays hold pre-rendered strings with the old colors baked in as ANSI codes, unlike the dashboard which re-renders every frame. `main` calls `styles.Apply(styles.LoadTheme())` at startup, and `New()` seeds the stamp from the same file so the first tick reports no spurious change. `Apply` rebuilds the package color vars (`Primary`, `Secondary`, `Subtle`, `Warn`, `Danger`, `Fg`, `Bg`, `Surface`, `SurfaceBright`, `BorderInactive`) and derived styles; helper funcs (`Pane`, `Divider`, `LatencyGraph`, …) read them at call time. Add new colors as `Theme` fields, not raw codes. Hex degrades to ANSI on non-TrueColor terminals.
- **Elm architecture.** Standard Bubble Tea Model/Update/View; keep them split across `internal/tui/{model,update,view}.go`.

## Architecture

```
main.go                     entry point: tea.NewProgram(tui.New(), WithAltScreen)
internal/
  types/types.go            domain models (Peer, LocalStatus, enums) — CLI-agnostic
  tailscale/tailscale.go    live adapter: status/ping/set/switch/login/logout → types structs
  styles/theme.go           Theme struct, DefaultTheme (Matrix Core), tri-schema TOML loader + ThemeStamp
contrib/tailtui.toml.tpl    Omarchy theme template (optional; renders tailtui.toml on theme switch)
  styles/styles.go          theme-derived styles (Apply), Divider/Bar/LatencyGraph helpers
  styles/pane.go            Pane(): sharp single-line box with title in the top border
  tui/
    model.go                Model struct, New(), Init(), selectedPeer()
    update.go               Update(): resize + quit; everything else delegated to the list
    peerlist.go             bubbles/list construction, custom 1-line delegate, sort logic
    overlay.go              help/routes/settings modal state machine + floating-modal compositor (x/ansi)
    settings.go             Advanced Settings modal: settingDefs (5 toggles → tailscale set flags), openSettings
    routing.go              Routing Management modal: openRouting + working copy, routingBody, list/input/confirm handlers, CIDR textinput, Command Room (tailscale set + clipboard)
    ssh.go                  SSH launcher modal: openSSH, nav/edit sub-modes, live command preview, renderSSHOverlay
    poll.go                 async cmds: status fetch + refresh tick, ping + ping tick, exit-node action
    view.go                 View(): computeLayout() + pane rendering + routesSummary
```

Key design points that span multiple files:

- **`types` is decoupled from any Tailscale wire format on purpose.** `Peer`/`LocalStatus` use semantic enums (`ConnType`, `NodeType`, `OS`) so the view picks glyphs/colors by switching on a type, never by string-matching CLI output. The `internal/tailscale` adapter maps wire data → these structs.

- **The Tailscale adapter (`internal/tailscale`) is the only place that knows the CLI/JSON shape.** Private wire structs (`status`/`node`/`user`) unmarshal a *subset* of `tailscale status --json`; only `types.LocalStatus`/`types.Peer` cross the package boundary, so nothing downstream sees a Tailscale-specific field. Mapping decisions: `ConnType` from `Online`+`CurAddr` (online & has current endpoint → DIRECT, online & no endpoint → RELAY via `Relay` DERP region, else OFFLINE); `NodeType` = exit (if `ExitNodeOption`) → subnet-router (if it has non-default `PrimaryRoutes`) → regular; `OffersExitNode`=`ExitNodeOption`, `IsActiveExitNode`=`ExitNode`; `OffersSSH`=`len(sshHostKeys) > 0` (the daemon only reports host keys for a node running the Tailscale SSH server — **advisory only**, since per-peer ACL permission is not exposed by the CLI, and it fails soft if upstream renames the key); `AdvertisedRoutes` = `PrimaryRoutes` minus the exit defaults (`0.0.0.0/0`, `::/0`); local `User` looked up via `Self.UserID` in the `User` map; local `LocalIP` = first RFC1918 host in `Self.Addrs` (the LAN IP isn't a top-level field); `LastSeen` humanized to a relative label ("Connected"/"3h ago"). **Peers are sorted by hostname in the adapter** before returning — map iteration is random, so without this the list would reshuffle every poll. Note: `tailscale status` carries **no per-peer Version**, so the `Peer.Version` field (and the PEER DETAILS `Version:` line) was **removed entirely in Phase 17** rather than displayed empty. Errors unwrap `*exec.ExitError` to surface the CLI's stderr.

- **Async, non-blocking data (`poll.go`).** `os/exec` must never run on the Update goroutine. `fetchStatusCmd()` is a `tea.Cmd` whose closure (run by Bubble Tea in its own goroutine) shells out under a `context.WithTimeout(refreshInterval-1s)` and returns a `statusMsg{local, peers, err}`. `Init()` returns `tea.Batch(fetchStatusCmd(), tickCmd())` — fetch once immediately, then `tickCmd()` (`tea.Tick`, `refreshInterval = 4s`) re-fires a fetch + reschedules itself every cycle. `Update` handles `statusMsg` via `applyStatus` and `tickMsg` by batching the next fetch+tick.

- **`applyStatus` folds a poll into the model without disrupting the user.** It stores `fetchErr` (kept on error, last good data stays on screen — never blanks), refreshes `m.local`, and rebuilds the list via `peers.SetItems(peerItems(...))`. It **preserves the highlighted node by hostname** (re-`Select`s it after the rebuild) so a 4s refresh never yanks your selection, and **skips the peer rebuild while `FilterState()==Filtering`** so a poll can't interrupt active `/` typing (the next tick reconciles). A fetch error renders as a red `> [ERROR] …` line atop the TERMINAL_LOGS pane.

- **Live latency (`tailscale ping`, `poll.go`).** A second ticker (`pingTickCmd`, `pingInterval = 2s`, started in `Init`) pings **only the highlighted online node** — `pingCmd(ip)` runs `tailscale ping --c 1 <ip>` off-thread and `tailscale.Ping` parses the `… in 137ms` tail (regex) into an int, returning an error (not a zero) when the node doesn't answer. `applyPing` appends the sample to `Model.latency` (a `map[ip][]int`, capped at `maxLatencySamples = 40`), drops failed pings, and updates that node's list item in place so the LATENCY HISTORY pane (and its MIN/AVG/MAX) refresh immediately. `applyStatus` re-injects `Model.latency` into the fetched peers via `withLatency`, so accumulated samples survive a 4s status refresh instead of resetting. History is keyed by Tailscale IP and accumulates per node, so navigating away and back keeps a node's graph. The **local node has no latency** — `tailscale ping` to self returns "is local Tailscale IP" (no RTT), so the LOCAL_NODE inline sparkline reads empty; the live graph is always the selected *peer's*. The adapter no longer synthesizes latency.

- **Exit-node toggle (`x`) is wired to the daemon, optimistically.** `toggleExitNode` still rewrites the list items immediately (instant feedback) **and** issues `setExitNodeCmd` → `tailscale.SetExitNode` (`tailscale set --exit-node=<ip>`, empty string clears). The optimistic flags hold until the next status poll reconciles the model with the daemon's `peer.ExitNode` truth: a successful change shows no flicker (optimistic == daemon), a failed one reverts on the next poll and its error (`actionMsg.err`) surfaces in the logs pane. Only `x` maps to this action (the former `t` alias was removed). The footer `[x] Exit Node` hint shows **only when the highlighted peer offers exit-node service**. (The wireframe-spec `p` ping-as-action is deferred — intentionally absent from the footer and help until implemented. The Advanced Settings modal is bound to uppercase **`S`**; lowercase `s` is the SSH launcher as of Phase 34.)

- **Intelligent peer sort (adapter-authoritative).** `mapPeers` sorts into four buckets — exit nodes (0), subnet routers (1), online regular peers (2), offline regular peers (3) — each alphabetical by hostname (`sortBucket`); offline nodes are pinned to the bottom. This replaced the earlier flat alphabetical sort. The tui's `sortPeers`/`nodeRank` stable pass is now redundant-but-order-preserving (specials are already first), kept only as a guard for any non-adapter item source.

- **Live routes modal.** `routesBody` renders the peer's real `AdvertisedRoutes` (its daemon `PrimaryRoutes`) with the gateway = the node's Tailscale IP, LATENCY = the node's live ping reading (`—` until a sample arrives), and a `[ ROUTING ]` (`StatusOK`) chip for every approved route. The old index-based mock columns (`routeLatency`/`routeStatus`, with fake DISABLED/CONFLICT/ERR variety) are gone. Styling/layout of the table is unchanged.
- **The peer list drives the details pane.** The middle pane is a `bubbles/list.Model`; `Model.selectedPeer()` reads `list.SelectedItem()`, and `View` renders that peer on the right every frame. There is no separate "selected index" state to keep in sync — the list is the single source of truth for selection.
- **`peerDelegate` renders one dense line per node** (`> 󰖟 [EXIT] amsterdam-exit ●`) instead of the default two-line delegate, to match the spec's high-density list. Exit nodes and subnet routers are stable-sorted to the top via `nodeRank`.
- **Custom search/filter (`search.go`, Phase 13) — NOT bubbles/list's built-in.** `list.SetFilteringEnabled(false)`; we own filtering for explicit fzf-style modes and panic-proof clamping. State: `Model.allPeers` (full sorted set, source of truth), `searchQuery`, `searchFocused`. Two modes: **Input** (`searchFocused`, box focused) and **Normal** (blurred). `Update` routes ALL keys to `updateSearchInput` while `searchFocused`, so nothing leaks to the command switch or the list keymap while typing:
  - **Input Mode:** printable runes type (incl. `j`/`k`); `↑`/`↓` and `Ctrl+j`/`Ctrl+k` navigate the filtered list (via `searchNav` → `selectClamped`); `backspace` edits; **`Enter`/`Esc` blur → Normal Mode, keeping the filter applied**.
  - **Normal Mode:** `/` (re)enters Input Mode; **`Esc` clears the filter** (full list) when one is applied; `j`/`k`/arrows navigate (`wrapNav` + list).
  Matching is case-insensitive fuzzy *subsequence* over `Peer.FilterValue()` (hostname+tags), preserving the special-node-first order. The search box is the NODES pane **title**: `SEARCH: <q>▌` in Input Mode (trailing block cursor) vs `FILTER: <q>` when blurred (no cursor) vs `FILTER NODES...` when inactive — so the cursor presence *is* the mode indicator.
- **Cursor clamping = crash fix (Phase 13).** Every cursor move funnels through `selectClamped(i)` (clamps to `[0, len(items)-1]`, safe on empty); `applyFilter` rebuilds the visible items and **resets the cursor to 0** so a shrinking match set can't leave a stale index, and `applyStatus` re-clamps after a refresh. This eliminates the out-of-bounds panic seen when filtering a long list (cursor far down) to a few items and then navigating.
- **Wrap-around navigation.** `wrapNav` intercepts the single-step nav keys (`j`/`k`, ↓/↑) in Normal Mode and jumps to the opposite end *only* when already at a boundary (top→bottom, bottom→top); otherwise it returns `handled=false` and the key falls through to the list so normal movement and pagination are untouched. Boundaries are computed over `list.VisibleItems()` (== the current filtered items) using `Index()`/`Select()`. Filtering is now synchronous (our own), so this path is fully exercisable by calling `Update` in isolation (unlike the old async bubbles filter).
- **Exit node state lives in the list items, not in `LocalStatus`.** `Peer.IsActiveExitNode` is the single source of truth; at most one peer has it set. `Peer.OffersExitNode` is a *capability* flag — `toggleExitNode` (`x`) is a no-op unless the highlighted peer offers exit-node service. When it does toggle, it rewrites every peer via `list.SetItem` — enabling the highlighted one and clearing the rest, or clearing all if it was already active. The dashboard's `Exit:` value is *derived* via `Model.activeExitNodeName()` (scans the list), so it stays in sync automatically; `LocalStatus.ExitNode` is left empty on purpose. The active node is marked in the list with a soft yellow `󰖟 exit` text label (`styles.ExitName`) — a minimal marker, not a filled chip.
- **Advertised routes.** `Peer.AdvertisedRoutes` holds subnet CIDRs (subnet routers only). When the selected peer has routes, `renderDetails` appends a yellow `[e] N advertised routes` hint line (`styles.Caution`). The PEER DETAILS pane is sized to `localNodeH` (≥ 8 inner rows after Phase 18's tightening), which comfortably fits the 6 identity rows (IDENTITY · OS · IP · Conn · Tags · Last Seen — the `Version:` row was dropped in Phase 17) + the hint, so it's never clipped by the pane's `MaxHeight`; when there's no hint the Pane just pads the bottom. Pressing `e` on such a peer opens the full routes overlay. (Clipped after the Phase 9 reshuffle, restored in Phase 10, and made symmetric — fixed height — in Phase 11.)
- **Advanced Settings modal (`S`, Phase 20 / rebound Phase 20.1).** A master/detail overlay (`stateSettings`) for toggling five live local-node preferences. Opened with **uppercase `S` (shift+s)** — lowercase `s` opens the SSH launcher (Phase 34), so the two are distinguished only by case; never collapse them. The LOCAL_NODE pane gains a second stacked action label **`[S] Advanced Settings`** (`renderSettingsButton`) under the `[c]` connect button; to keep the top panes flush, `localNodeH` went **10 → 11** (and `detailsH = localH` follows automatically, so PEER DETAILS grows by the same row — the dropped/extra space is absorbed by its Pane padding). The modal is **rendered directly from model state** (`m.prefs` / `m.settingCursor`) each frame via `renderSettingsOverlay` — **not** the shared viewport — so toggling re-renders instantly. It composites two opaque, Surface-filled side-by-side `modalBox`es over the still-visible base (`overlayCenter`): **left** `ADVANCED_SETTINGS` (focused → Primary border) lists the five toggles with a `❯` cursor and a colored checkbox (`[x]` green `StatusOK` when on / `[ ]` dim when off); **right** `DESCRIPTION` (dim `BorderInactive` border) shows the highlighted toggle's help text (`wrapText`) and the exact `tailscale set --<flag>=<next>` command. The five toggles map to `tailscale set` flags via `settingDefs` (`settings.go`, data-driven get/set closures over `types.Prefs`): Accept Subnet Routes (`--accept-routes`), Allow LAN Access (`--exit-node-allow-lan-access`), Run Tailscale SSH (`--ssh`), Accept MagicDNS (`--accept-dns`), Shields Up (`--shields-up`). **State is read live** from `tailscale.GetPrefs` (`tailscale debug prefs` → JSON `ipn.Prefs` subset: `RouteAll`/`ExitNodeAllowLANAccess`/`CorpDNS`/`RunSSH`/`ShieldsUp` → `types.Prefs`), fetched in `Init` and on every `[S]` open (`fetchPrefsCmd` → `prefsMsg`). Keys (consumed entirely by the modal): **`j/k`** move `settingCursor` (clamped); **`Space`** flips the highlighted pref **optimistically** and fires `setPrefCmd` → `tailscale.SetPref` (`tailscale set --<flag>=<bool>`) off the UI thread; **`Esc`/`q`** close. The `prefActionMsg` logs the outcome and **re-fetches prefs** to reconcile the checkbox with the daemon (reverting a failed optimistic flip), mirroring the exit-node reconciliation pattern. `modalTitledTopColor` generalizes the opaque `┌─┤ TITLE ├─┐` top border to a caller-chosen focus color (the existing `modalTitledTop` now delegates to it with `Primary`); `modalBox` clips each content line (`MaxWidth`) so an over-long flag can never wrap and break the flush overlay.

- **Routing Management modal (`R`, Phases 21–23).** A floating overlay (`stateRouting`) surfacing — and locally editing — this node's advertised exit-node + subnet routes (applied via the Command Room, `stateRoutingConfirm` — see the execution constraint above), **styled after ACCOUNT_MANAGEMENT** (it reuses the same viewport-modal chrome: `⌐ ROUTING_MANAGEMENT ¬` tab title, top/bottom `modalDivider`s, and the two-column `gridLine`/`accountKey` keymap). The LOCAL_NODE pane's action label **`[R] Routing`** sits on the **same horizontal row** as `[S] Advanced Settings` (`renderActionButtons` centers `[S] Advanced Settings    [R] Routing` on one line, below the `[c]` connect button) — grouped horizontally in Phase 21.1 so the pane isn't bottom-heavy, which also let `localNodeH` revert to **11** (it stays locked to `detailsH = localH`, so PEER DETAILS keeps the same height and the top borders stay flush). Opened with **uppercase `R` (shift+r)** — lowercase `r` is explicitly ignored in `Update` (a no-op `case "r"`) and reserved for a future routing action. `routing.go` owns it: `openRouting` **snapshots** `prefs.AdvertiseExitNode`/`AdvertiseRoutes` into an **editable working copy** (`Model.routingExitNode` / `routingRoutes`) and builds the shared viewport from `routingBody`; the modal renders the **fixed Exit Node (Advertise) toggle** (`[ON]`/`[OFF]`) at cursor index 0, then each **advertised subnet route** (cursor 1..N) — or a dim **`No custom routes advertised.`** placeholder. The **highlighted row uses the inverted `styles.AccountActive` bar** (green bg, dark text), exactly like the accounts modal's active session.

  **Phase 22 — local editing (no CLI execution).** All edits mutate the working copy only — `m.prefs` (the daemon's last-known truth) is never touched; the diff is applied to `tailscale set` in Phase 23. The modal has two sub-modes:
  - **List mode** keys: **`j/k`** navigate (`routingCursor`, clamped to `routingItemCount()` = 1 + len `routingRoutes`); **`Space`** toggles `routingExitNode` (only on item 0); **`d`** removes the highlighted route (clamps the cursor when the last item goes); **`a`** enters input mode; **`Esc`/`q`** close. The keymap shows `[J/K] NAVIGATE · [A] ADD ROUTE · [SPACE] TOGGLE · [D] REMOVE · [ESC] CLOSE`.
  - **Input mode** (`routingInputMode`) embeds a **`bubbles/textinput`** (`routingInput`, themed to the modal surface) with the prompt `Enter CIDR (e.g., 192.168.1.0/24):`, replacing the keymap with `[ENTER] CONFIRM · [ESC] CANCEL`. **`Enter`** validates via `net.ParseCIDR` — valid → append to `routingRoutes`, exit input mode, highlight the new route; invalid → set `routingInputErr` (red prompt flash) and clear, staying in input mode (no crash). **`Esc`** cancels back to list mode. Input mode owns **all** keys (including `esc`/`q`) via an early guard in `updateOverlay` → `updateRoutingInput`, so the global close handler can't fire mid-entry.

  Any edit sets `routingDirty`; a late `prefsMsg` re-snapshots the working copy from fresh prefs **only while clean and not mid-entry**, so it never clobbers edits. `refreshRoutingOverlay` re-renders **and resizes** the viewport (the body height changes between sub-modes, so a stale height would clip the editor). Data is read live from `prefs` (`fetchPrefsCmd` on `Init` and every `[R]` open).

- **Overlay state machine.** `Model.state` (`stateMain`/`stateHelp`/`stateRoutes`/`stateAccounts`/`stateLogs`/`stateSettings`/`stateRouting`/`stateRoutingConfirm`/`stateSSH`) gates input and rendering. `internal/tui/overlay.go` owns it: `openHelp`/`openRoutes`/`openAccounts`/`openLogs`/`openRouting` size a shared `bubbles/viewport` and set its content (the **Advanced Settings** modal, `openSettings`, is the exception — it renders **directly from model state** each frame via `renderSettingsOverlay`, not the viewport). `updateOverlay` handles keys while an overlay is open — `esc`/`q` close any overlay (`?` also closes help, `v` also closes the log overlay). The viewport-modal footer hint (`renderOverlay`) shows the **`-- KEYBOARD INPUT MODE ACTIVE --`** banner **only** in a genuine inline text-input state — for viewport-backed overlays that means the routing CIDR editor (`routingInputMode`); read-only / navigation overlays (Help, Accounts, the routing list) show a plain `[Esc] Close` hint instead (Phase 23.1). The SSH launcher is a **model-state renderer** and bypasses that switch entirely, so it emits the equivalent banner itself, gated on `sshInputMode` (Phase 34). Any future inline editor must do one or the other — the banner marks "keys are being typed, not interpreted". Help/routes/logs **scroll** the viewport; the **accounts, settings, and routing modals consume their own keys** (navigation + actions), nothing falling through. The isolation rule lives in `Update`: when `state != stateMain`, keys route to `updateOverlay` and never reach the peer list. **Exceptions:** while the routing modal is in CIDR **input mode**, an early guard hands *every* key (including `esc`/`q`) to `updateRoutingInput` so the close handler can't fire mid-entry; and `stateSSH` is routed to `updateSSH` before the global close in **both** its sub-modes (a username may legitimately contain a `q`). Overlays re-size on `WindowSizeMsg` via `resizeOverlay`. The accounts modal (`l`, `accountsBody`) shows a green active-session bar + inactive rows + a two-column action grid, backed by **live** profile data; the settings (`S`) and routing (`R`) modals are detailed in the constraints above.

- **Live account management (Phase 14 + sudo coverage 24.2 / 24.3).** Profiles come from `tailscale.Accounts` (`tailscale switch --list --json` → `[]types.Account{ID, Email, Active}`, active sorted first). Fetched in `Init`, on `[l]` open, and after every account action (`fetchAccountsCmd` → `accountsMsg` on success, `accountsLockedMsg` on the profile-lock error — see below — which re-`SetContent`s the modal if open). On Linux the profile store is root-owned, so **all four mutating actions** run as `tea.ExecProcess` invocations wrapped in `sudo` (closing the modal — `state = stateMain` — *before* dispatch so the post-`ExecProcess` repaint lands on the dashboard, not a stale overlay): **`Enter`** switches to the highlighted *inactive* profile (`switchAccountCmd` → `sudo tailscale switch <id>`); **`a`** adds an account (`addAccountCmd` → `sudo tailscale login` — interactive for both the sudo password and the auth URL); **`d`** removes the highlighted *inactive* profile (`removeAccountCmd` → `sudo tailscale switch remove <id>`); **`l`** logs out (`logoutCmd` → `sudo tailscale logout`). Active-profile guards: `Enter`/`d` on the active row are no-ops. All four return `accountActionMsg{desc, err}`, which logs the outcome and batches `fetchAccountsCmd` + `fetchStatusCmd` + `fetchPrefsCmd` on success (login can change which prefs the daemon will return for this user) and `fetchAccountsCmd` on error. The orphaned `tailscale.SwitchAccount` / `RemoveAccount` / `Logout` adapter functions were deleted across 24.2 / 24.3; only the `Accounts` reader remains. Styling/dimensions of the modal are unchanged — only the data flow and key routing.

- **Profile-store lock detection (Phase 24.3).** `fetchAccountsCmd` is re-fired on every refresh and account action, so an unprivileged session would flood the log ring with the recurring `Access denied: profiles access denied` line. The guard: `isProfilesAccessDenied(err)` (case-insensitive substring match on "profiles access denied" / "access denied") classifies the error and the command returns a **distinct `accountsLockedMsg{}`** instead of a generic `accountsMsg{err: …}`. The Update handler for `accountsLockedMsg` sets `Model.profilesLocked = true`, clears `m.accounts` (no stale list from a prior elevated run lingering behind the hint), resets the cursor, refreshes the modal if open — and **logs nothing** (the lock flag is the only side-effect). A successful `accountsMsg` clears the flag, so freshly-granted perms drop the hint on the next refresh. `accountsBody` substitutes the empty-state line for the wrapped (`wrapText`, modal-width aware) hint **"Profile store locked. Run tailTUI with sudo to view and manage accounts."** when the flag is set; the divider + two-column action grid stay in place (single-text substitution, no layout change), and `[a]` still works since it pre-elevates via sudo itself.
- **Floating modals (compositing).** `View` always renders the full base layout first, then `renderOverlay(base)` blits the modal on top so the dashboard/list/details stay visible behind it. lipgloss v1 has no overlay primitive, so `overlayCenter` does it by hand: for each background row covered by the modal it keeps the left columns (`ansi.Truncate`), splices in the modal line, then resumes the background past the modal (`ansi.TruncateLeft`) — both are ANSI-aware and carry SGR state across the cut, with explicit `\x1b[0m` resets isolating the three segments. This is why `internal/tui` depends directly on `github.com/charmbracelet/x/ansi`.
- **Brutalist sharp panes (`styles.Pane`).** Every logical region is a `styles.Pane(title, body, w, h, focused)` — a SHARP single-line box (`┌─┐│└┘`, `lipgloss.NormalBorder` geometry built by hand) with the title embedded in the top border: `┌─┤ LOCAL_NODE ├────┐`. No rounded corners anywhere (Matrix Core is strictly sharp/brutalist). `Pane` renders to exact OUTER w×h (border + `Padding(0,1)` included); use `styles.ContentWidth(outer)` for the usable inner width. The title is left-aligned in the top border to match the mockups (the spec text says "centered" but the images are left).
- **Layout is centralized in `computeLayout` (Phase 11 grid).** Both `Update` (which sizes the list via `SetSize`) and `View` (which draws) read the same geometry via `m.layout()`. The screen is: header bar · a body band · footer bar. The body is two columns joined by a 1-col gutter, **each summing to `bodyH` so they join flush** (`JoinHorizontal(Top, …)`): **left = LOCAL_NODE (fixed `localNodeH`) over the FILTER NODES list (flex)**; **right = PEER DETAILS (fixed) over LATENCY HISTORY (flex) over the TERMINAL_LOGS tail (fixed `logsHeight`)**. Each column's flex pane is computed as the remainder, so the sums are exact. **PEER DETAILS is LOCKED to the LOCAL_NODE height (`detailsH = localH`)** for horizontal symmetry — the bottom border under the two top panes aligns across the screen regardless of selection, and short content (or the missing routes hint) is absorbed by the Pane's own bottom padding. When the terminal is short, `localH` (and thus `detailsH`) is clamped toward `localFloor` so the flex node list keeps `nodesFloor` and LATENCY keeps `minLatencyH`. The list's pane height (`nodesH`) depends only on `localH`+`bodyH`, **not** on the selected peer, so navigation never re-sizes the list — only `WindowSizeMsg` does. `WindowSizeMsg` is intercepted (not forwarded to the list) so the list gets the *pane* size.

- **Flush-layout invariants (Phase 9 border fix).** The full render must be EXACTLY `width × height`; any single line wider than the terminal wraps and pushes the layout down, scrolling the top borders off the alt-screen (the reported regression). Two guards enforce this: (1) `styles.Pane` clips its content with `MaxHeight`/`MaxWidth` (lipgloss `Height`/`Width` only pad, never truncate — an over-tall body, e.g. a wrapped log line, otherwise grows the pane); (2) `styles.Bar` (header/footer) clips the left segment and caps the whole line at `width`, so a long hint string can't overflow. Keep both when touching panes or bars.
- **Border = focus, color = state.** Only the focused pane (the peer list / NODES) gets the bright `Primary` border; all others use the dim `BorderInactive`. The selected list row is a full-width `SurfaceBright` (`#333b34`) highlight bar with a leading `❯` pointer (`styles.Selected`, built from plain text so the bar paints uniformly). Content colors follow state: accent = online/direct/routes, warning (`Caution`, yellow) = exit node/relayed, error = conflict, dim = offline/secondary. `●`/`○` mark online/offline.
- **Latency graph.** The LATENCY HISTORY pane uses `styles.LatencyGraphArea(values, width, height)` — a **multi-row** vertical bar chart that fills the (now flexible) pane: bar *height* is scaled to the series min/max using eighth-block glyphs for sub-row precision, while bar *color/weight* encodes absolute latency via `latencyColor` — faint accent <30ms, solid accent 30–59, bold warning 60–99, bold error ≥100ms. `renderLatency` sizes the chart to the pane's remaining inner height (`latencyH − stats − spacer`); it falls back to the single-row `LatencyGraph`/`LatencyGraphWidth` at height 1. The compact dashboard "Exit Latency" readout keeps the single-color `styles.Sparkline`. All pull colors from the active theme (`Primary`/`Warn`/`Danger`) — never hardcoded.
- **Color consistency audit (theme-purity invariant).** There must be **no hardcoded color anywhere outside `theme.go`** — no hex, no `lipgloss.Color("<n>")`. Every styled element references the theme via the derived `styles.*` vars/styles. Common leak spots and the rule: list glyphs must be colored (`styles.IconOnline`/`IconOffline`), not left raw (a raw glyph inherits the terminal's default fg and silently ignores the theme); both ping graphs read theme colors; modal content uses the `styles.Modal*` surface styles. To verify after a change: `grep -rnE 'lipgloss\.Color\("(#|[0-9])' internal/` must return nothing outside `theme.go`.
- **Opaque modals (no bleed) with tonal depth.** Overlays are fully opaque rectangles filled with the elevated `Surface` color (`#1a211b`, a tonal layer above the `Background`), giving the "floating" depth the style guide asks for. Every content span uses a `styles.Modal*` style that bakes in `Surface`; every line is padded full width via `modalLine`/`styles.ModalFill` (and `modalRow` for two-column rows, which surface-fills the gap); the container sets explicit `Width`/`Height` **including** padding plus `Background`+`BorderBackground` so the whole bounding box overwrites the view behind it. A sharp `NormalBorder` (Primary) frames it, with the title as a `⌐ TITLE ¬` tab bar. `overlayCenter` splices it over the base. The routes overlay is a `DESTINATION/GATEWAY/LATENCY/STATUS` table with `StatusOK`/`StatusWarn`/`StatusErr` chips; the help overlay is grouped key tables. (Verify opacity after edits: render the overlay over an all-`X` background and assert no `X` survives inside the border bounds.)

Before changing any interaction, consult the keybinding matrix in `_designs/00_WIREFRAME_SPEC.md` and the matrix below — keys are scoped global vs. context (list/modal) and must not collide. The wireframe spec is **historical**, not binding: several of its keys were deliberately superseded (`t` → `c` for connect, `p` reinterpreted, and `Enter` is **not** bound to SSH — see Phase 34). Record divergences here rather than silently drifting.

## Phase log

- **Phase 1** — skeleton: Model/Update/View, three-pane responsive lipgloss layout, mock data, ANSI theme.
- **Phase 2** — `bubbles/list` peer pane: navigation (`j/k`/arrows), `/` fuzzy filter, dynamic details pane driven by selection, special-node-first sorting.
- **Phase 2 refinement** — exit node toggle (`x`): `Peer.IsActiveExitNode` flag, single-active invariant, yellow `EXIT` list chip, and dashboard `Exit:` derived from list state.
- **Phase 2 completion** — strict exit-node capability (`OffersExitNode`), advertised routes + details-pane summary (max 5 + "more" hint), and the overlay state machine: help (`?`) and scrollable routes (`e`) modals with keybinding isolation.
- **UI refresh** — minimalist restyle: rounded borders, focus-colored pane borders (bright = focused, subtle = inactive), no-fill modals with bright rounded border + colored title, `Padding(0,1)` breathing room, icon/text spacing, and a gutter-bar list selection (replacing the solid block).
- **Phase 4 — Theme Engine** — dropped the strict-ANSI rule for TrueColor hex; central `styles.Theme` + default "Stitch" palette; refactored all styles through `Apply`; premium per-bar `LatencyGraph` with faint/normal/bold-warning/bold-error color nuance.
- **Phase 4.1 — Native Omarchy theme** — loader now parses the system Omarchy TOML (`~/.config/omarchy/current/theme/colors.toml`, go-toml/v2), mapping `accent`/`foreground`/`background`/`color0-15` onto the `Theme` struct, with graceful per-field fallback.
- **Phase 4.2 — Theme-purity audit + opaque modals** — themed the previously-raw list glyphs; confirmed zero hardcoded colors.
- **Phase 5 — Matrix Core master design** (`_designs/`) — exact YAML-frontmatter palette; sharp/brutalist `styles.Pane` (single-line borders, title in top border) replacing the rounded `Box`; `Surface`/`SurfaceBright` tonal layers; `❯` + surface-bright list selection; surface-filled tabular modals (routes table, grouped help); width-filling latency graph. State machine and navigation unchanged — styling only.
- **Phase 4.2 — Theme-purity audit + opaque modals** — confirmed zero hardcoded colors; themed the previously-raw list glyphs (`IconOnline`/`IconOffline`); made help/routes modals a fully opaque theme-`Background` fill so nothing bleeds through.
- **Phase 6 — Real data integration** — `internal/tailscale` adapter shells out to `tailscale status --json` (`os/exec`, private wire structs → `internal/types`); async `tea.Cmd` fetch + `tea.Tick` 4s refresh in `poll.go`; `Update`/`applyStatus` fold `statusMsg` into the model (selection preserved by hostname, filter-safe, last-good-data on error, error line in logs). Mock node data dropped from initial state; latency graphs, logs, and accounts remain mocked.
- **Phase 7 — Live latency, routes & action wiring** (logic only, no style/layout changes) — `tailscale ping` ticker feeds a per-node live latency history (`Model.latency`, `applyPing`, re-injected across status polls via `withLatency`); routes modal (`routesBody`) shows real `PrimaryRoutes` + live ping latency, mock columns removed; exit-node toggle (`x`/`t`) executes `tailscale set --exit-node=…` optimistically with poll reconciliation; adapter peer sort upgraded to exit → subnet → online → offline buckets (alphabetical). `mock.SyntheticLatency` removed.
- **Phase 8 — Log system, footer & Exit Latency** — real FIFO log ring (`appendLog`, 500-cap) capturing exit-node action results (incl. real `tailscale set` stderr as `[ERROR]`) and status connectivity transitions; bottom pane reduced to a 2-line tail; `[v]` full-history log overlay (`stateLogs`) in an opaque pane-style titled modal (`┌─┤ TERMINAL_LOGS ├─`); footer restored with `[x] Exit` (+ `[v] Logs`); LOCAL_NODE "Latency" → "Exit Latency" showing live ping to the active exit node (also pinged each tick), `N/A` when none.
- **Phase 9 — Layout reorg, sudo execution & border fix** — fixed the missing-top-border regression by clipping `styles.Pane` (`MaxHeight`/`MaxWidth`) and `styles.Bar` (overflowing footer wrapped → scrolled the top off); reorganized the grid (full-height NODES list left; right column = LOCAL_NODE + PEER DETAILS + flex LATENCY + TERMINAL_LOGS tail); multi-row dynamic-height latency chart (`LatencyGraphArea`); `[O]` operator setup via `tea.ExecProcess` (interactive `sudo tailscale set --operator=$USER`).
- **Phase 10 — Layout correction & data restoration** — final two-column grid: left = LOCAL_NODE (fixed) over FILTER NODES list (flex); right = PEER DETAILS (fixed) over LATENCY HISTORY (flex) over TERMINAL_LOGS tail (fixed). Restored the `[e] N advertised routes` hint for subnet routers (clipped in Phase 9). Same Phase 9 clipping guards and sharp borders.
- **Phase 11 — Symmetry & log overlay proportions** — locked PEER DETAILS height to LOCAL_NODE (`detailsH = localH`) so the top panes' bottom borders align across the screen (routes hint / short content absorbed by Pane padding); widened the `[v]` log overlay to `logOverlayWidth` (~85% of terminal, capped 120) so log lines don't wrap. Aesthetic/dimension only — clipping guards, styles, and the multi-row graph untouched.
- **Phase 11.5 — Log level syntax highlighting** — `styles.LogLevelColor` maps ERROR/INFO/WARN/DEBUG to theme colors (red/green/yellow/secondary); both the tail (`logTailLine`) and overlay (`logLine`) color the level chip, dim the timestamp, and keep the message default. Foreground-only — no layout/dimension changes.
- **Phase 12 — Interactive connection toggle & UX polish** — state-aware LOCAL_NODE button (`[c] Connect` green / `[c] Disconnect` yellow) wired to `[c]` → `tailscale up`/`down` via `tea.ExecProcess` (interactive, no sudo), logged + auto-refreshed; footer made dynamic (`[c]` hint + `CONNECTED`/`DISCONNECTED` status) and the `[x]` hint clarified to `[x] Exit Node`.
- **Phase 13 — Search UX, discoverability & crash fix** — replaced bubbles/list's built-in filter with a hand-rolled fzf-style search (`search.go`): Input/Normal modes, `Ctrl+j/k` + arrow nav while typing, two-stage Esc (blur→clear), search box in the NODES title with a mode-indicating block cursor. Fixed the out-of-bounds cursor panic via `selectClamped` everywhere + cursor-reset-to-0 on filter change. Dynamic Input-Mode footer hint and a SEARCH group in the `[?]` help overlay.
- **Phase 14 — Live account management** — wired the `[l]` modal to real profiles: `tailscale.Accounts` (`switch --list --json`) replaces `mock.Accounts`; `Enter` switch / `a` login (ExecProcess) / `d` remove / `l` logout routed to the CLI with active-profile guards; results logged + auto-refresh status & accounts. Modal styling/dimensions untouched.
- **Phase 15 — Rebrand to tailTUI** — header title `TAILSCALE_TUI_V1.0` → `tailTUI` (`appName`); `appVersion` (`v1.0.0`) pinned to the far-right of the footer (still one flush line); user-facing "Tailscale TUI" strings/comments updated; README rewritten as a public-facing landing page.
- **Phase 16 — Module rename, env scrub & license** — Go module `github.com/Phundahl/tailscaleTUI` → `github.com/Phundahl/tailtui` (all imports + `go.mod`; repo dir renamed to `tailtui`); theme env var `TAILSCALE_TUI_THEME` → `TAILTUI_THEME`; README deep-scrubbed (install `go install github.com/Phundahl/tailtui@latest`, `TAILTUI_THEME` documented, **Acknowledgments** added); added MIT `LICENSE` (© 2026 Phundahl). Repo hygiene only — no TUI/behavior changes.
- **Phase 17 — Exit State, Version removal & doc alignment** — the LOCAL_NODE `State:` field (formerly the local node's own connectivity) was renamed **`Exit State:`** and re-pointed at the active exit node: `renderExitState` reports the route status (`DIRECT`/`RELAY`) of the `IsActiveExitNode` peer straight from `tailscale status --json`, or a dim `N/A` when none is active. The PEER DETAILS **`Version:` line was removed** (status has no per-peer version), along with the now-unused `types.Peer.Version` field and its `mock` bindings. Both panes keep their exact fixed heights/widths (`localNodeH`/`detailsH` unchanged — the dropped row is absorbed by the Pane's bottom padding), so the flush grid alignment is untouched. README + CLAUDE.md updated to match.
- **Phase 18 — Layout tightening & production log cleanup** — trimmed the two top panes by exactly one row each: `localNodeH` 11 → 10 (and `detailsH = localH` follows, so both bottom borders stay flush) and removed the empty padding line above the LOCAL_NODE `[c]` connect button; PEER DETAILS' extra bottom pad row is reclaimed automatically by the height drop. The flex bottom panes (NODES list / LATENCY) absorb the reclaimed row; the grid separator stays unbroken and resize behavior is unchanged. Removed the last development mock: deleted the `internal/mock` package and stopped seeding the log ring — `Model.logs` now **starts empty** and fills only with real runtime events.
- **Phase 20 — Advanced Settings modal & local prefs** — a master/detail overlay (`stateSettings`, opened with `[S]` — see Phase 20.1) for toggling five live local-node preferences. `tailscale.GetPrefs` (`tailscale debug prefs`) reads the real `ipn.Prefs` subset into `types.Prefs`; `tailscale.SetPref` drives each `tailscale set --<flag>=<bool>` off the UI thread (optimistic flip + `prefActionMsg` re-fetch to reconcile). Entry point: a new `[S] Advanced Settings` label stacked under `[c]` in LOCAL_NODE, with `localNodeH` 10 → 11 (and `detailsH = localH` following) so the top panes' bottom borders stay flush. The modal renders directly from model state via `renderSettingsOverlay` — two opaque side-by-side `modalBox`es (`ADVANCED_SETTINGS` focused / `DESCRIPTION` dim) composited over the still-visible base; `j/k` navigate, `Space` toggles, `Esc` closes. Added `modalTitledTopColor`/`modalBox`/`wrapText` helpers and a `settings_test.go` smoke suite (flush invariant, overlay opacity-over-base, navigate/toggle/clamp, checkbox state).
- **Phase 20.1 — Settings hotkey swap** — moved the Advanced Settings modal from `s` to **uppercase `S`** (label `[S] Advanced Settings`, help/footer/docs updated); lowercase `s` is now an explicit no-op in `Update`, freeing it for the upcoming SSH-as-action. String/keybinding change only — no layout dimensions altered.
- **Phase 21 — Routing Management modal (layout & parse)** — a read-only floating overlay (`stateRouting`, opened with `[R]`) styled after ACCOUNT_MANAGEMENT, surfacing the node's advertised exit-node toggle (`[ON]`/`[OFF]`) and advertised subnet routes (or a `No custom routes advertised.` placeholder), with the highlighted row in the inverted `AccountActive` bar and a `[J/K] NAVIGATE` / `[ESC] CLOSE` keymap. State is parsed live from `tailscale debug prefs` (new `prefsWire.AdvertiseRoutes` → `types.Prefs.AdvertiseExitNode` + `AdvertiseRoutes`, exit defaults split out). Entry point: a `[R] Routing` label in LOCAL_NODE, `localNodeH` 11 → 12 (`detailsH = localH` follows) to keep the top borders flush. `j/k` navigate (`routingCursor`, clamped); `Esc` closes. Lowercase `r` is an explicit no-op (reserved). **No text input / deletion / `tailscale set` execution this phase** — deferred. Added `routing.go` + `routing_test.go` (hotkey, flush-overlay-over-base, placeholder, nav clamp).
- **Phase 21.1 — Routing button regroup & height revert** — moved `[R] Routing` onto the **same line** as `[S] Advanced Settings` (`renderActionButtons`, replacing the two single-button helpers) so the LOCAL_NODE pane isn't bottom-heavy and PEER DETAILS no longer carries an extra empty padding row; reverted `localNodeH` **12 → 11** (`detailsH = localH` follows, so both halves shrink by one row and stay flush). Pure visual layout tweak — no state, data, or keybinding changes; the flush-symmetry test asserts 9 LOCAL_NODE content rows again.
- **Phase 22 — Routing Management interaction & CIDR input** — made the routing modal editable **locally** (no CLI execution — Phase 23). `openRouting` snapshots prefs into a working copy (`routingExitNode`/`routingRoutes`); list mode adds **`Space`** (toggle exit-node advertise on item 0), **`d`** (remove highlighted route, cursor-clamping), and **`a`** (enter input mode). Input mode embeds **`bubbles/textinput`** with a `net.ParseCIDR`-validated CIDR prompt — `Enter` appends valid input (rejects + red-flashes invalid without crashing), `Esc` cancels; an early `updateOverlay` guard routes all keys (incl. esc/q) to `updateRoutingInput` so the close handler can't fire mid-entry. `routingDirty` guards a late `prefsMsg` from clobbering edits; `refreshRoutingOverlay` re-renders and resizes the viewport across sub-modes; the bottom keymap is dynamic (`NAVIGATE/ADD/TOGGLE/REMOVE/CLOSE` vs `CONFIRM/CANCEL`). Added `routing_interaction_test.go` (toggle, delete-clamp, add valid/invalid, esc-cancel-not-close, dynamic keymap). New dep used: `github.com/charmbracelet/bubbles/textinput`.
- **Phase 22 addendum — Smart Edit/Undo pre-fill** — `[d]` now stashes the removed CIDR in `Model.lastDeletedRoute`; the next `[a]` pre-fills the editor with it (`SetValue` + `CursorEnd`, cursor at end), giving a one-keystroke pop-back (undo) or fix-a-typo flow. The buffer resets on modal (re)open and the pre-filled value still passes the Phase 22 `net.ParseCIDR` validation on `Enter`. Tests: `TestRoutingDeletedRoutePrefillsAdd`, `TestRoutingPrefillEditableAndValidated`.
- **Phase 23 — Routing execution, Command Room & clipboard** — wired the routing modal to the daemon via a transparent confirmation overlay (`stateRoutingConfirm`). `Enter` in the routing list opens the **Command Room** (`renderRoutingConfirmOverlay`) titled `[ CONFIRM ROUTING CHANGES ]`, showing `ABOUT TO EXECUTE:` + the exact `tailscale set --advertise-exit-node=<bool> --advertise-routes=<csv>` (assembled by `tailscale.AdvertiseCommandString`/`AdvertiseArgs`, empty routes → `--advertise-routes=` to clear), safely wrapped (`wrapCommand`/`hardWrap`), plus the dim-italic Admin Console approval reminder. Keymap: **`Enter`** APPLY (`setRoutingCmd` → `tailscale.SetRouting` off-thread, closes to `stateMain`, `routingActionMsg` logs + refreshes status/prefs); **`c`/`C`** COPY (`copyRoutingCmd` via `github.com/atotto/clipboard`, flashes `✓ Copied!` from `routingCopied`/`clipboardMsg`, stays put); **`Esc`/`q`** BACK to the list. `updateOverlay` routes confirm keys *before* the global close so Esc = back, not quit. New dep: `github.com/atotto/clipboard`. Tests: `internal/tailscale/advertise_test.go` (command assembly incl. empty-routes clear) + `routing_confirm_test.go` (open/flush/back/apply/copy state transitions).
- **Phase 23.1 — Overlay visual cleanup** — fixed the footer banner leak and a first pass at the cursor styling (no state/keybinding changes). The `-- KEYBOARD INPUT MODE ACTIVE --` footer banner leaked into read-only overlays (Help, Accounts, routing list); it's now gated on `routingInputMode`, with a plain `[Esc] Close` hint elsewhere (Settings/Command Room/Logs use their own render paths and were never affected). Also pre-swapped the cursor style for the `Style.Reverse(true)` cursor render. Tests: `overlay_chrome_test.go`.
- **Phase 23.2 — The real "black box" fix (textinput Width padding)** — the massive black block was the textinput's own Width padding, not the cursor: with a `Placeholder` set, an empty field renders via `placeholderView`, which pads the remaining Width with **raw, unstyled spaces** (default near-black bg) that sit mid-line after a reset — so neither an outer `Background(Surface).Render()` wrap nor `ModalFill`'s `Width().Background()` recolors them (both only color the padding *they* append). Fix: **drop the in-field `Placeholder`** so the empty field pads via the main path's `TextStyle` (Surface bg) — confirmed with the real `textinput` under a forced TrueColor profile. The Phase 23.1 cursor styling was kept (it's the one genuinely necessary workaround — prevents the 1-char reverse-video cursor block) and the now-redundant `PlaceholderStyle` was removed. New regression test (`TestRoutingInputNoRawBlackPadding`) forces TrueColor and asserts the rendered field has no `\x1b[0m`-then-raw-spaces run; verified it fires when a placeholder is reintroduced.
- **Phase 23.3 — Visible cursor** — the 23.2 cursor style (`Foreground(Surface).Background(Primary)`) reversed into a Primary glyph **on Surface**, which on an empty field (space glyph) blended in completely → invisible. Fixed to `Foreground(Primary).Background(Bg)` so `bubbles/cursor`'s `Style.Reverse(true)` displays a **solid bright Primary block** (the displayed bg = `Style`'s Foreground = Primary). No state/keybinding change. Render-level test `TestRoutingCursorIsVisibleBlock` (forced TrueColor) asserts the visible cursor cell is reverse-video and carries the Primary color.
- **Phase 24 — Release v1.1.0, changelog & roadmap** — bumped `appVersion` `v1.0.0` → `v1.1.0` (the only definition, in `view.go`; footer + README mockup updated); added a "What's New in v1.1.0" changelog and a "Roadmap" section to the README. Docs/housekeeping only — no logic, state, or keybinding changes.
- **Phase 24.1 — Interactive-exec return handling & operator resolution** — tightened the three return paths for the `tea.ExecProcess` flows (which were already in place; the symptom was post-suspend state + refresh, not background execution). (1) **Operator resolution**: `currentUser()` now prefers `os/user.Current` (a `getuid()` syscall — authoritative regardless of launcher / `sudo` / daemonization) and only falls back to `$USER` if that fails, so the `--operator=` flag handed to `sudo tailscale set` is always the real invoking user rather than an inherited / empty env var. (2) **Modal close before login**: `[a]` in `stateAccounts` now sets `state = stateMain` **before** dispatching `addAccountCmd`, so the post-`ExecProcess` repaint lands on the dashboard rather than a stale modal frame and the auth-URL output isn't drawn behind an overlay during the suspension. (3) **Comprehensive return sync**: `operatorDoneMsg` now batches `fetchStatusCmd` + `fetchAccountsCmd` + `fetchPrefsCmd` (was status only) and explicitly resets `state = stateMain`, and `accountActionMsg` (the login return path) likewise adds `fetchPrefsCmd` — so any newly-granted perms / unlocked account or pref data show up immediately. No visual / layout / keybinding changes; tests pass.
- **Phase 24.2 — Sudo for profile-store mutations** — on Linux the local profile store is root-owned, so the daemon returns `Access denied: profiles access denied` for `tailscale login` / `tailscale switch` regardless of the operator role granted by `[O]`. **`addAccountCmd` is now `sudo tailscale login`** (was `tailscale login`) — still via `tea.ExecProcess`, so the sudo password prompt and the subsequent auth URL print are both interactive and visible in the suspended terminal. **`switchAccountCmd` was refactored from a background `tea.Cmd` (calling `tailscale.SwitchAccount`) into a `tea.ExecProcess` running `sudo tailscale switch <id>`**, for the same reason: switching mutates the profile store and silently fails without root, and the user needs to see the sudo prompt. The now-orphaned `tailscale.SwitchAccount` adapter was deleted (zero callers). All Phase 24.1 return handling is preserved: both flows still close the modal before dispatch (`overlay.go` `enter`/`a` set `state = stateMain` first) and `accountActionMsg` still batches `fetchAccountsCmd` + `fetchStatusCmd` + `fetchPrefsCmd` on return. `removeAccountCmd` / `logoutCmd` are intentionally **not** touched — they remain background `tea.Cmd`s; if they surface the same profile-store error in practice they'll need the same wrap, but no symptom has been reported yet so we haven't introduced a sudo flash where one isn't needed. No visual / layout / keybinding changes; tests pass.
- **Phase 24.3 — Total profile-store sudo coverage & graceful lock handling** — finished the profile-store sudo treatment and stopped the unprivileged-session log flood. **(1) Sudo consistency**: `removeAccountCmd` (`sudo tailscale switch remove <id>`) and `logoutCmd` (`sudo tailscale logout`) were refactored from background `tea.Cmd`s into `tea.ExecProcess` invocations, matching the 24.2 add/switch pattern; `[d]` and `[l]` in the accounts modal now also set `state = stateMain` before dispatch (consistent close-before-suspend) so all four account actions behave identically. The orphaned `tailscale.RemoveAccount` / `tailscale.Logout` adapters were deleted (zero callers — `cliError` stays, it's still used by `SetPref`/`SetRouting`). **(2) Profile-lock detection**: a new `isProfilesAccessDenied(err)` helper (case-insensitive substring match on "profiles access denied" / "access denied") classifies the daemon's response from an unprivileged session. `fetchAccountsCmd` now branches on this — a recognized lock returns a **distinct `accountsLockedMsg{}`** instead of the generic `accountsMsg{err: …}`. **(3) Model lock flag**: `Model.profilesLocked` carries the state across renders. The `accountsLockedMsg` handler sets it true, clears `m.accounts` (so a stale list from a prior elevated run can't linger behind the hint), resets the cursor, and **logs nothing** — the poll re-fires on every refresh and every action, so logging would paper the ring with the same line every few seconds; the lock flag is the message's only side-effect. A successful `accountsMsg` clears the flag, so freshly-granted perms drop the hint on the next refresh. **(4) Modal hint substitution**: `accountsBody` swaps the "No accounts. Press [a] to add one." empty-state line for the wrapped (`wrapText`, modal-width aware) hint **"Profile store locked. Run tailTUI with sudo to view and manage accounts."** when `m.profilesLocked` is true. The divider + two-column action grid below stay put — single-text substitution, no layout change — and `[a]` continues to work (it pre-elevates via sudo itself). No keybinding changes; tests pass.
- **Phase 24.4 — Documentation alignment for the sudo / lock cycle** — docs-only polish closing out the 24.x release. **README.md** gained a new **"Permissions & sudo"** section between Keybindings and Theming, contrasting (1) daily monitoring + node/routing/settings operations that only need the Tailscale operator role (one-time `[O]`, then no further sudo) with (2) the four Account Management actions (`a`/`Enter`/`d`/`l`) that suspend the TUI for an interactive sudo prompt because the Linux profile store is root-owned — plus a callout that an unprivileged launch shows the "Profile store locked" hint inside `[l]` and suppresses the recurring error from the log ring. **CLAUDE.md** stale spots were corrected to match the 24.x reality: the Phase 14 "Live account management" constraint block now lists all four actions as `sudo … via tea.ExecProcess` (was: switch/remove/logout as background `tea.Cmd`s) and references the lock-detection / `accountsLockedMsg` path; a new sibling constraint block documents the lock-state model + suppression invariants; the keybinding-matrix row for the accounts modal mirrors the new command flow. No Go / test changes — markdown only.
- **Phase 25 — Global Linux positioning & Omarchy theme reframing** — docs-only repositioning ahead of the public release, broadening the framing from Omarchy-centric to "any modern Linux distro" without disowning the aesthetic roots. Architectural premise (confirmed before editing): the binary is a single statically-compiled Go executable, the only runtime call-outs are `tailscale` / `sudo` / `pbcopy`/`wl-copy`/`xclip`/`clip` (all distro-independent), and the Omarchy TOML loader is conditional with graceful fallback to the built-in "Matrix Core" palette — so cross-distro compatibility is structural, not aspirational. **README.md** changes: (1) the **Installation** section now leads with an explicit compatibility paragraph — single statically-compiled binary, no runtime deps beyond the `tailscale` CLI, runs identically on Ubuntu / Debian / Fedora / Arch / NixOS / openSUSE / Alpine, no packaging tweaks needed; (2) the **Requirements** line was lightly rephrased (Go 1.26+ is build-time only; TrueColor recommended-not-required, ANSI fallback explicit); (3) the **Theming** section was renamed **"Theming & aesthetic (Omarchy roots)"** and rewritten as three paragraphs — the visual identity ("Matrix Core" palette, translucent-friendly, brutalist), the optional Omarchy integration (auto-adopts `~/.config/omarchy/current/theme/colors.toml` when present), and an explicit *"the Omarchy binding is purely cosmetic, not a requirement"* paragraph listing terminals it renders cleanly in (Alacritty, Kitty, Ghostty, WezTerm, Foot, GNOME Terminal, Konsole, iTerm2, Windows Terminal). The `TAILTUI_THEME` override and ANSI degradation are preserved. No Go / test / settings / asset changes — markdown only.
- **Phase 26 — Anonymized mock mode & VHS demo tape** — a non-destructive demo / screenshot path for the public README. The env var **`TAILTUI_MOCK=1`** flips the entire `internal/tailscale` adapter to an in-memory fixture without touching the live paths: a new `mock.go` owns the writable state (`mockState`, guarded by `mockMu`) and every adapter entry point (`Status` / `Accounts` / `Ping` / `GetPrefs` / `SetPref` / `SetExitNode` / `SetRouting`) gets a single-line `if mockEnabled { return mock…(); }` early-return at the top, so live behavior is byte-identical when the env var is unset. The mock fixture is **7 fictional anonymous nodes** (`exit-node-se`, `dc-subnet-router`, `home-nas`, `srv-web-01`, `db-cluster-prod`, `dev-macbook`, `field-laptop`) covering all OS / NodeType / Conn / online combinations, **2 mock accounts** (`demo@tailtui.dev` active, `personal@tailtui.dev` inactive), a sensible `Prefs` starting state (incl. two advertised subnet routes), and a **deterministic per-IP latency wave** (`mockBaseline` + 12-step `mockWaveOffset`) that drives both `Ping` and the seeded `MockLatencySeed()` history. The TUI's `tea.ExecProcess` flows — which bypass the adapter entirely — also branch on `tailscale.MockEnabled()`: `addAccountCmd` / `switchAccountCmd` / `removeAccountCmd` / `logoutCmd` / `connectCmd` / `operatorSetupCmd` each call into the matching `Mock…` mutator and return their `*Msg` synchronously instead of suspending the terminal, so the demo never tries to open sudo / spawn `tailscale login`. `model.New` seeds `Model.latency` from `MockLatencySeed()` when mock is on, so the LATENCY HISTORY pane is populated on frame one (live ping ticks then append normally on top). New artifacts: **`demo.tape`** at the repo root (Charm VHS script — hidden launch then dashboard hold → list navigation → `[S]` Advanced Settings → `[R]` Routing → `[l]` Account Management showcase → quit; the mock env var travels via the invoking shell per 26.1) and a short README callout under Installation explaining the `TAILTUI_MOCK=1 go run .` workflow. Live paths unchanged, all existing tests pass.
- **Phase 26.1 — VHS tape syntax correction & README hero-image swap** — docs-only follow-up to Phase 26. **(1) Tape correction**: the `Set Env TAILTUI_MOCK 1` directive added to `demo.tape` in Phase 26 is **not valid VHS syntax** and was removed; the env var must travel via the invoking shell instead (**`TAILTUI_MOCK=1 vhs demo.tape`**). The tape's header comment was updated to document the correct invocation and explicitly note that `Set Env` is intentionally **not** used. Going forward, do **not** reintroduce `Set Env TAILTUI_MOCK 1` to the tape. **(2) README hero image**: the static ASCII terminal mockup block that lived directly under the introductory paragraph was replaced with the live demo image tag `![tailTUI Live Demo](assets/demo.gif)`, pointing at the GIF produced by `vhs demo.tape`. Generating `assets/demo.gif` is a separate manual step (run VHS locally with the mock env var); the README now expects that file to exist when the repository is published. No Go / test changes — markdown + tape comment only.
- **Phase 26.2 — Privacy clean-up: pseudonym-only attribution** — anonymized every personal-name reference across the repo ahead of publication. Three text touches: **LICENSE** copyright holder line now reads `Copyright (c) 2026 Phundahl`; **README.md**'s Acknowledgments paragraph and License footer both attribute the project to `Phundahl`; **CLAUDE.md**'s Phase 16 log entry parenthetical was updated to match. After the sweep, a case-insensitive repo-wide grep for the prior real-name fragments returns only the GitHub-username import path `github.com/Phundahl/tailtui` (the public handle, which is supposed to stay). **Standing rule going forward — strict pseudonym enforcement**: the only attribution form anywhere in this codebase is `Phundahl`. No first / full / real names in source-file headers, code comments, docstrings, markdown docs, the phase log, or commit messages — including when *describing* a fix that anonymized one (paraphrase the change instead of quoting the old value). No Go / test / functional changes this phase — text-only.
- **Phase 27 — Omarchy 4 ("Quattro") theme support: dual location, dual schema & light mode** — the theme loader had gone silently dead on upgraded machines. Omarchy 4 changed **both** halves of the contract: the current-theme symlink moved `~/.config/omarchy/current/theme/` → `~/.local/state/omarchy/current/theme/`, and `colors.toml` swapped its flat terminal palette (`color0`–`color15`) for **semantic slots** (`mode`, `selection`, `muted`, `lighter_background`, `dark_background`, `red`/`yellow`/`orange`/`green`, …). `LoadTheme` found nothing, fell back to Matrix Core without a word, and the UI stopped tracking the desktop. **(1) Path probing**: `themeCandidates()` returns the v4 location then the legacy one (or a lone `TAILTUI_THEME` override); `LoadTheme` walks them and takes the first that yields a usable palette, and `ThemePath()` now reports the file actually read rather than a hardcoded guess. **(2) Schema detection**: `omarchyV4` and `omarchyLegacy` wire structs each expose `hasMarkers()`, checking only the keys **exclusive** to that generation — the three shared keys (`accent`/`foreground`/`background`) are deliberately excluded so they can't misclassify a file; detection is by content, not filename, so an override of either vintage maps correctly. A file with only the shared three still applies them and defaults the rest. **(3) Light mode**: new `Theme.Mode` field (`ModeDark`/`ModeLight`) read from the v4 `mode` key, used to flip which slot becomes `Surface` — `lighter_background` when dark, `dark_background` when light — because the elevated surface has to move away from the canvas in whichever direction the canvas sits. Light themes (`catppuccin-latte`, `flexoki-light`, `white`) previously rendered as an inverted mess. **(4) Warning slot**: `yellow`, falling back to `orange` for the themes that omit it. **Tests** (`internal/styles/theme_test.go`, the package's first): verbatim fixtures from real shipped themes cover v4 dark, v4 light (asserting `Surface` takes `dark_background` and *not* `lighter_background`), the legacy schema, orange fallback, partial/ambiguous files, malformed/empty/missing files, and the path-priority + override rules; `TestInstalledOmarchyThemesMapCompletely` sweeps every `colors.toml` installed on the machine (22 themes) and fails if **any** palette slot is empty or silently equal to its Matrix Core default — the tripwire for the next upstream schema change. It skips cleanly where Omarchy isn't installed. No TUI / layout / keybinding changes; `styles.Apply` and every consumer are untouched.
- **Phase 28 — CI workflow & release-config repair** — `.github/workflows/release.yml` was the **only** workflow and it fires solely on `v*` tags, so nothing verified a commit until a release was already being cut, and a broken `.goreleaser.yaml` could only surface as a failed release job *after* the tag existed. Added `.github/workflows/ci.yml`: runs on pushes to `main`, on every pull request, and via `workflow_dispatch`; `concurrency` cancels superseded in-flight runs; `contents` permission is read-only. Two jobs — **`test`** (a `gofmt -l` gate, a `go mod tidy` drift gate, `go build ./...`, `go vet ./...`, and **`go test -race ./...`**; the View layer is TTY-free so the suite runs headless, and `-race` covers the async `tea.Cmd` status/ping polling paths) and **`goreleaser-config`** (`goreleaser check`, so config errors fail on a PR instead of on a tag). Both resolve Go via `go-version-file: go.mod` rather than `stable`, so CI tracks the pinned toolchain instead of drifting on release day. **The new check immediately caught three real defects** in `.goreleaser.yaml`, every one of which would have failed the release job only after a tag was pushed: `nfpms.files` → **`nfpms.contents`** (`files` is a v1 key and is rejected outright by the v2 schema — the hard failure); `archives.format` → **`archives.formats`** (deprecated singular); and `snapshot.name_template` → **`snapshot.version_template`** (deprecated — note `goreleaser check` exits non-zero on deprecations, not just schema errors). Also dropped `-extldflags "{{.Env.LDFLAGS}}"` from the build ldflags — it required an `LDFLAGS` env var to be set at release time and is a no-op under `CGO_ENABLED=0` (no external linker to pass it to) — and translated the one non-English comment. **Test hermeticity is a requirement for CI to stay green**: every styles test uses `t.Setenv` for `HOME`/`TAILTUI_THEME`, and `TestInstalledOmarchyThemesMapCompletely` (which globs the machine's real Omarchy theme dir) **skips** when none is installed. Don't add a test that assumes Omarchy, a TTY, or a live `tailscale` daemon without an equivalent skip guard.
- **Phase 29 — Release v1.2.0 & version plumbing** — cut the release covering everything since the (never-published) `v1.1.0` tag: the 24.x sudo account flows, Phase 26 mock mode + VHS demo, the 26.2 privacy pass, Phase 27's Omarchy 4 theme support, and Phase 28's CI. **Version plumbing fix**: `.goreleaser.yaml` stamps `-X main.version={{.Version}}`, but `main.go` had **no `version` var** and `appVersion` was a `const` in `internal/tui/view.go` — so the ldflag silently did nothing (Go ignores `-X` on a missing symbol) and a tagged build would have shipped whatever was hardcoded. Now `main.go` declares `var version string` (empty for `go build`/`go run`) and calls **`tui.SetVersion(version)`** first thing in `main`; `appVersion` became a **var** (dev-build literal `v1.2.0`) and `SetVersion` ignores an empty value and normalizes a bare `1.2.0` → `v1.2.0`, since goreleaser's `{{.Version}}` drops the prefix. `appVersion` remains the single definition — set it there, never duplicate it. Added `internal/tui/version_test.go` (normalization table incl. prerelease + empty, plus a render test asserting the stamped version actually reaches the footer while staying flush). `.gitignore` was rewritten to cover the compiled `/tailtui` binary (previously **committed**, ~6.5 MB, removed from history in the same cycle), test/coverage output, and editor/OS noise, while preserving the existing `_designs/` allow-list. README gained a `ci` status badge under the tagline and a "What's New in v1.2.0" section above the v1.1.0 one. No layout / state / keybinding changes.
- **Phase 30 — tailTUI theme schema, Omarchy template & live theme reload** — closed the one gap Phase 27 could not: some mappings are genuinely undecidable in code. `osaka-jade` defines `yellow = #459451` (a green), so mapping `yellow`→Warning makes exit-node markers nearly indistinguishable from the online green — but preferring `orange` breaks `retro-82`, where orange *is* the accent. **(1) tailTUI's own schema**: a third wire struct `tailtuiTheme` with one key per `Theme` field, tried **before** both Omarchy schemas, and probed as `tailtui.toml` **ahead of** `colors.toml` inside each theme directory. Keys are disjoint from the Omarchy vocabularies so detection stays key-based; `mode`/`background` are shared and therefore excluded from `hasMarkers()`. **(2) Omarchy template**: `contrib/tailtui.toml.tpl`, installed by the user into `~/.config/omarchy/themed/`. Omarchy's `omarchy-theme-set-templates` renders every `*.tpl` there into the **active theme directory** (`$NEXT_THEME_DIR/<name>`, which becomes `~/.local/state/omarchy/current/theme/`) on every theme switch — i.e. straight into the directory the loader already probes, so no new path was needed. Templates get the full palette (semantic slots, `color0`–`color15`, `mode`, `bright_`/`dark_`/`light_` variants) plus `_strip`/`_rgb` modifiers and a `{{ mix a b 30% }}` blend. **(3) Live reload**: `styles.ThemeStamp()` (path+mtime) plus `checkThemeCmd`, batched onto the **existing** `refreshInterval` tick — no fsnotify, no new dependency — so switching the desktop theme re-colors a running tailTUI within one tick. The critical constraint: the command **returns** the parsed `Theme` and never calls `styles.Apply` itself, because `Apply` rewrites package-level vars `View` reads; applying happens in `Update`, on the Elm goroutine. The handler records the stamp even on no-change (so a file appearing/vanishing is caught) and rebuilds viewport-backed overlays via `resizeOverlay()`, whose content would otherwise keep stale ANSI colors. Zero-config behavior is untouched: with no template installed, `colors.toml` is read exactly as before. **Tests**: own-schema mapping, light mode, three-way detection non-ambiguity, `tailtui.toml`-over-`colors.toml` precedence, fallback when no template exists, `ThemeStamp` change tracking and empty state (`theme_test.go`); palette applied + stamp recorded + logged, unchanged-is-quiet, overlay rebuilt (forcing TrueColor, since a TTY-less test strips every SGR and both renders would otherwise compare equal), and `New()` stamp seeding (`theme_reload_test.go`).
- **Phase 31 — Release v1.3.0** — bumped `appVersion` `v1.2.0` → `v1.3.0` (still the single definition in `view.go`) for Phase 30's live theme switching + tailTUI theme schema. **Packaging gap closed**: the README told users to `cp contrib/tailtui.toml.tpl …`, which is wrong for anyone who installed from the AUR / `.deb` / `.rpm` — they have a binary package, not a checkout. The template is now installed to **`/usr/share/tailtui/tailtui.toml.tpl`** by the packaging, and the README gives both paths. Any future user-facing file added under `contrib/` needs the same treatment, or the docs silently break for package users. Also decoupled `version_test.go` from the real version string (it now asserts on an obviously-arbitrary literal), so a release bump no longer looks like it requires a test edit — the test exercises `SetVersion`'s normalization, not the current version. README gained a "What's New in v1.3.0" section; the theming docs and the Phase 30 entry shipped with the feature itself, which is the rule: **docs ride in the feature PR, and the release PR carries only version, changelog, and phase log**.
- **Phase 32 — Install documentation for every distribution route** — the README's Installation section still offered only `go install` and a source build, despite v1.2.0/v1.3.0 publishing `.tar.gz`/`.deb`/`.rpm` for amd64+arm64 and an AUR package existing since v1.1.0 — the recommended path for most users was undocumented. Rewrote it into four routes (AUR, `.deb`/`.rpm`, prebuilt tarball, from source), leading with the packaged ones since they need no Go toolchain. **The rule this enforces**: every install route must be able to follow every other instruction in the README. The live-theme-switching section previously offered `/usr/share/tailtui/…` (packages) or `contrib/…` (clone), which strands anyone who ran `go install` — they have neither, so a third `curl` line fetching the template from `raw.githubusercontent.com` was added. Also documented the two `go install` caveats: the binary lands in `$GOBIN`/`$GOPATH/bin`, which is not necessarily on `PATH`, and a source build installs the binary alone. Docs-only — no Go, test, or packaging changes, and no version bump (nothing user-facing changed in the program itself).
- **Phase 33 — Neutral wording for the theme-mapping rationale** — the README justified the `tailtui.toml` template in two places by naming one shipped Omarchy theme and addressing the reader in the second person: the v1.3.0 changelog bullet ("You decide the ambiguous mappings … `osaka-jade` defines its `yellow` as a green") and the Theming section's template paragraph ("… fixes it, for your themes, permanently"). On a public repo that reads as pointed at a specific theme and its author rather than as documentation of a general problem. Both passages now state the case generically — *some palettes define their `yellow` as a green, which leaves exit-node markers nearly indistinguishable from the online color* — while keeping the concrete `warning = "{{ orange }}"` fix, which is the part a reader actually needs in order to write their own template. **The rule this sets**: public-facing docs describe the *class* of problem, not a named third-party theme that fails at it; internal notes may stay specific — `CLAUDE.md`'s Phase 30 entry still names `osaka-jade` deliberately, because the mapping decision it records only makes sense against the concrete case. Docs-only — no Go, test, or packaging changes, and no version bump (nothing user-facing changed in the program itself).
- **Phase 34 — SSH launcher (`s`)** — bound lowercase `s`, reserved since Phase 20.1, to an `SSH_LAUNCH` modal (`stateSSH`, `internal/tui/ssh.go`) that previews and then runs `tailscale ssh [user@]host` for the highlighted peer via `tea.ExecProcess`. **Capability detection cost nothing**: `tailscale status --json` already carries per-peer `sshHostKeys`, populated only when that node runs the Tailscale SSH server, so one new wire field on `node` yields `types.Peer.OffersSSH` with no extra CLI call — advisory only, since per-peer `CapMap` is always `null` and therefore ACL permission is unknowable client-side (the modal carries a static caveat instead, the structural twin of the Command Room's Admin Console reminder). **Two sub-modes, forced rather than preferred**: an always-focused username field and a `[c]` copy key are mutually exclusive, so the modal opens in nav mode with the field blurred and pre-filled (`currentUser()`, then session-sticky via `sshLastUser`) and `[e]` enters the editor — reusing the routing list/input split verbatim, including the early `updateOverlay` guard that hands *every* key (incl. `esc`/`q`) to the editor so a username containing a `q` survives. **Gating is deliberately looser than `[x]`**: offered on any *online* peer, because a node without Tailscale SSH may still run a plain sshd; the modal shows `[ADVERTISED]`/`[NOT ADVERTISED]` up front. Capability is marked on the right of the peer-list row (plain `ssh`, no Nerd Font glyph — an unverified codepoint would render as tofu and desync the width) and as a green `[s] Tailscale SSH` line in PEER DETAILS (6 identity rows + 2 hints = 8, inside the 9 inner rows, so `localNodeH` is untouched). Pure `SSHTarget`/`SSHArgs`/`SSHCommandString` sit beside the Advertise pair, with the preview built *from* the argv so it cannot drift. Execution takes **no timeout** (an interactive session is unbounded) and pre-flights `exec.LookPath("ssh")`; a non-zero exit is logged **WARN with the code**, not ERROR, because `ssh` returns the *remote* command's status. **Three adjacent fixes rode along**: `newModalInput` now owns the textinput styling contract for both editors, which exposed a pre-existing latent bug — those styles are captured by value at construction, so a live theme reload left a stale cursor color baked in; `restyleModalInput` re-applies them from `resizeOverlay` (regression test verified to fail without it). `clipboardMsg` gained a `kind` (routing as the zero value) so a second copier cannot flash the wrong indicator. And `[O] Operator` now retires from the footer once `prefs.OperatorUser` matches the current user — a one-time step shouldn't hold permanent footer space, and retiring it is what buys room for `[s] SSH`. Also repointed CLAUDE.md's two references to the long-deleted `design-spec.md` at `_designs/00_WIREFRAME_SPEC.md`. **Deliberate divergence from that spec**: it binds `s / Enter`; only `s` is bound, because `Enter` is the most reflexively-pressed key on a list and the accident cost is suspending into a remote shell. New `internal/tailscale/ssh_test.go` + `internal/tui/ssh_test.go` (incl. `newModelWithPeers`, the peer-seeding helper the suite lacked); `TestSettingsHotkeyIsUppercase` was strengthened — it would have kept passing for an accidental reason, since the shared helper builds an empty peer list.
- **Phase 35 — Release v1.4.0** — bumped `appVersion` `v1.3.0` → `v1.4.0` (still the single definition, in `view.go`) for Phase 34's SSH launcher. README gained a "What's New in v1.4.0" section; the launcher's own docs — the feature bullet, the keybindings row, the Permissions & sudo note about ACLs, the Installation caveat that `tailscale ssh` wraps the **system `ssh` client**, and the Phase 34 log entry — all shipped with the feature itself, per the standing rule: **docs ride in the feature PR, and the release PR carries only version, changelog, and phase log**. `version_test.go` needed no edit (Phase 31 decoupled it from the real version string). **Live verification closed the Phase 34 gap**: the `tea.ExecProcess` suspend/restore path — the one thing the headless suite cannot reach, since no peer on the development tailnet advertises `sshHostKeys` — was confirmed by hand against a real target, including the return to the dashboard when the session exits.
- **Phase 36 — Release checklist** — the v1.4.0 cycle shipped correctly but the **AUR package sat at 1.3.0-1 afterwards**, because the bump lived only in the maintainer's memory: it is a separate git repo (`aur@aur.archlinux.org:tailtui.git`), invisible from this one, with no CI and nothing in the release flow pointing at it. Added a **Release checklist** section to CLAUDE.md making the order explicit and the dependencies between steps stated — feature PR (code + its docs + phase entry) → release PR (version + changelog only, branched *after* the feature merges, or the changelog describes work the tag won't carry) → annotated tag → **verify the published artifacts** → AUR bump. The verify step is not ceremony: it re-checks the `-X main.version` ldflag, which Phase 29 shipped as a silent no-op because Go ignores `-X` on a missing symbol, and it re-checks that the `.deb` still carries `contrib/tailtui.toml.tpl` per the Phase 31 packaging rule. The AUR step spells out `makepkg --printsrcinfo > .SRCINFO` (the AUR rejects a mismatched `.SRCINFO`) and a real `makepkg -f` build before pushing, since a broken PKGBUILD reaches users with no CI in between. The Git-workflow bullet on releases now defers to the checklist instead of restating a subset of it. **goreleaser's `aurs:` automation was evaluated and rejected**: it generates a `-bin` package that installs the prebuilt binary, whereas this PKGBUILD builds from source with Arch's hardening flags, so adopting it would mean either renaming the package (orphaning the name users have installed) or silently swapping every user onto an unhardened binary. Recorded in the checklist so it isn't re-litigated from scratch. Docs-only — no Go, test, or packaging changes, and no version bump.
- **Phase 36 addendum — AUR RPC caches** — the v1.4.0 AUR push succeeded (the AUR's own cgit served the new `.SRCINFO` immediately, and the package page showed `1.4.0-1`), but the **RPC endpoint `/rpc/v5/info` kept reporting `1.3.0-1`** afterwards. Consulted on its own, that is indistinguishable from a failed push, and the tempting reaction — re-push, or start editing a PKGBUILD that was already correct — is the wrong one. The checklist's AUR step now names the package page and cgit as the authoritative post-push checks, and the RPC as the one that lags.
- **Phase 37 — Privacy guard in CI** — hardened the standing pseudonym rule (Phase 26.2) by making it a build gate instead of a review convention. `scripts/privacy-check.sh` plus a `privacy` CI job (a required status check) fail the build on absolute `/home/<user>` paths, tailnet MagicDNS names that aren't obviously fictional, email addresses other than the pseudonym's noreply one, and a commit author that isn't the pseudonym. Design notes worth keeping: the checks are **structural rather than a denylist of real values**, since a guard naming what it hides would defeat itself — which is also why the script excludes itself from its own scan; and the authorship check exists because **commit metadata is invisible to any scan of the working tree**, while being just as public as file content. Both were verified in both directions — every check confirmed to fire on a planted violation and to pass on a clean tree — and two false positives were allowlisted in the process (the `aur@aur.archlinux.org` git remote, and `noreply@github.com` where it appears in the guard's own documentation). Paired with a standing convention the script cannot enforce on its own: **test fixtures come from the mock package's fictional vocabulary, never from live daemon output.** Docs, script and CI only — no Go or packaging changes, and no version bump.
- **Phase 38 — CHANGELOG.md** — four stacked `## What's New in vX.Y.Z` sections had accumulated at the top of the README: **81 lines, 19% of the file**, sitting between the intro and `## Features`, so a visitor scrolled the entire release history before reaching what the tool does or how to install it. Moved them **verbatim** into a new root `CHANGELOG.md` under dated `## vX.Y.Z — YYYY-MM-DD` headings, newest first; the README keeps a trimmed **latest-release-only** section linking to it. `## Features` moved from line 113 to 47 and the README dropped 428 → 362 lines. Two findings shaped it: **v1.1.0 exists nowhere else** (no tag, no GitHub release — that section was its sole record, so moving without a destination would have deleted it), and **GitHub Releases are not a substitute** (goreleaser generates them from raw `git log`, so each body is 40-char SHAs and commit subjects; v1.2.0's is a 24-line dump of every commit in repo history). Kept the existing narrative bullets rather than converting to strict *Keep a Changelog* Added/Changed/Fixed categories — that would mean rewriting prose that reads better than the categories would. Feeding curated prose into the release notes was considered and **deferred**: it adds a hand-written step to every release. The **release checklist was updated in the same PR** — it had hard-coded the README "What's New" section as a required release artifact, and a stale checklist is exactly how the next release rebuilds the wall. Also repaired a double *and* and a mid-sentence wrap in `## Status`, both introduced when the v1.4.0 clause was bolted on. Docs only — no Go, no packaging, no version bump.
- **Upcoming (next major cycle — see the README Roadmap)** —
  - **Tailscale Serve & Funnel management**: visual port forwarding to securely expose local services to the tailnet (`tailscale serve`) or the public internet (`tailscale funnel`), driven from keyboard overlays in the existing modal style.
  - **Connection diagnostics**: deep-dive into peer connection health — DERP-relay vs. direct routing and the signals to debug a flaky link (likely from `tailscale status --json` endpoints + `tailscale ping`/`netcheck`).
  - **ACL tag management**: machine identities and `--advertise-tags` for production server environments (provision/audit tagged nodes), most likely a new modal mirroring the Routing Management staging + Command Room confirmation flow.
  - Smaller parked items: `p` ping-as-action; real local-node latency (e.g. via `netcheck` DERP RTT); surfacing `Self.CapMap["https://tailscale.com/cap/ssh"]` as an SSH-permission warning (deferred from Phase 34).

## Keybindings (implemented)

Keys are **scoped**. In the **main view** the global hotkeys below apply. While **any overlay is open**, `Update` routes every key to `updateOverlay` (the global hotkeys do **not** fire) — see the modal-specific table — and the only thing that crosses back to the main view is closing the overlay. `ctrl+c` always quits.

### Global hotkeys (main view)

| Key | Action |
| :-- | :-- |
| `j`/`k`, ↑/↓ | Move selection in the peer list (wraps around at top/bottom) |
| `/` | Open search (Input Mode); type to fuzzy-filter (hostname + tags). `↑↓`/`Ctrl+j`/`Ctrl+k` navigate while typing; `Enter`/`Esc` apply (blur); `Esc` again (in list) clears |
| `c` | Toggle the local tailnet connection — runs `tailscale up`/`down` interactively (shows auth URLs); button + footer reflect live state |
| `x` | Toggle highlighted peer as the active exit node — executes `tailscale set --exit-node=…` (only if it offers exit; off if already active). Footer hint `[x] Exit Node` shows only on exit-capable peers |
| `e` | Open the **Routes** overlay (`stateRoutes`) — only on a peer with advertised routes |
| `l` | Open the **Accounts** modal (`stateAccounts`, live profiles) |
| `S` | Open the **Advanced Settings** modal (`stateSettings`; uppercase / shift+s) |
| `R` | Open the **Routing Management** modal (`stateRouting`; uppercase / shift+r) |
| `v` | Open/close the **Logs** overlay (`stateLogs`) |
| `O` | Operator setup — suspends the TUI and runs interactive `sudo tailscale set --operator=$USER` |
| `?` | Open/close the **Help** overlay (`stateHelp`) |
| `esc` | In the main view: clear an applied search filter (otherwise no-op) |
| `q` | Quit the app (ignored while the search box is focused / filtering) |
| `s` | Open the **SSH launcher** (`stateSSH`) for the highlighted peer — any **online** peer; a no-op when offline or nothing is selected |
| `r` | **Reserved no-op** — explicitly ignored, held for a future routing action; never falls through to the list |
| `ctrl+c` | Quit (always, even mid-search or with an overlay open) |

### Modal-specific hotkeys

Inside an overlay, `esc`/`q` close it (except the Routing CIDR editor — see below). Other keys are scoped to that modal only:

| Modal (state) | Keys |
| :-- | :-- |
| Help (`stateHelp`) | `j/k`·arrows·page scroll the viewport · `?`/`esc`/`q` close |
| Routes (`stateRoutes`) | `j/k`·arrows·page scroll the viewport · `esc`/`q` close |
| Logs (`stateLogs`) | `j/k`·arrows·page scroll the viewport · `v`/`esc`/`q` close |
| Accounts (`stateAccounts`) | `j/k` navigate · `Enter` switch to highlighted (inactive) profile (`sudo tailscale switch …` via `tea.ExecProcess`) · `a` add (`sudo tailscale login` via `tea.ExecProcess`, interactive for the auth URL) · `d` remove highlighted (inactive) profile (`sudo tailscale switch remove …`) · `l` logout (`sudo tailscale logout`) · `esc`/`q` close. All four mutating actions close the modal (`state = stateMain`) before dispatch; when the profile store is locked the modal renders the "run with sudo" hint and `accountsLockedMsg` polling is log-suppressed |
| Advanced Settings (`stateSettings`) | `j/k` navigate the 5 toggles · `Space` toggle (`tailscale set --<flag>=<bool>`, optimistic + reconcile) · `esc`/`q` close |
| Routing — **list mode** (`stateRouting`) | `j/k` navigate (toggle + routes) · `Space` toggle exit-node advertise (item 0 only) · `a` enter CIDR input mode · `d` remove highlighted route (cursor clamps) · `Enter` open the **Command Room** confirmation · `esc`/`q` close. Edits stay local until applied in the Command Room |
| Routing — **CIDR input mode** (`stateRouting`, `routingInputMode`) | type to edit the `bubbles/textinput` · `Enter` confirm (validated by `net.ParseCIDR`; invalid → red flash + clear, stays in input mode) · `Esc` **cancel** the entry (returns to list mode — does **not** close the modal). Input mode owns every key, so `q`/`esc` can't close the modal mid-entry. The field is **pre-filled with the last `[d]`-deleted route** (pseudo-undo) with the cursor at the end |
| SSH launcher — **nav mode** (`stateSSH`) | `e` edit the username · `Enter` **CONNECT** (closes the modal, then hands the terminal to `tailscale ssh` via `tea.ExecProcess`) · `c`/`C` copy the command (flashes `✓ Copied!`, stays put) · `esc`/`q` close |
| SSH launcher — **edit mode** (`stateSSH`, `sshInputMode`) | type to edit the `bubbles/textinput` · `Enter` confirm (validated by `validSSHUser`; invalid → red flash, stays) · `Esc` **cancel the edit** (returns to nav mode — does **not** close the modal). Edit mode owns every key, so `q` types a literal `q` |
| Routing — **Command Room** (`stateRoutingConfirm`) | `Enter` **APPLY** — runs `tailscale set --advertise-…` async, closes the modal, refreshes status+prefs · `c`/`C` **COPY** the command to the clipboard (flashes `✓ Copied!`, stays put) · `Esc`/`q` **BACK** to the routing list without applying |

Keys from the wireframe spec not wired: `p` (ping-as-action, deferred). **Deliberate divergence:** that spec binds `s / Enter` to an SSH session; only `s` is bound. `Enter` is the most reflexively-pressed key on a list and the accident cost here is suspending the TUI into a remote shell, so it is held for a future "open the selected peer" action (the roadmap's Connection Diagnostics).

## Git workflow (required)

Adopted in the v1.2.0 cycle; `main` is a published, public branch.

- **Never commit directly to `main`.** Branch first: `feat/…`, `fix/…`, `ci/…`, `chore/…`, `docs/…`. Keep branches short-lived and single-purpose.
- **Every change lands through a pull request**, squash-merged, branch deleted. CI (`test` + `goreleaser-config`) gates the merge — see Phase 28.
- **Rebase onto the latest `main` before opening a PR**, so the PR is tested against current `main` rather than a stale base.
- **Order dependent work so the safety net lands first.** CI went in ahead of the feature PRs precisely so they were gated by it.
- **Scope a PR to files it truly needs.** When several changes touch the same doc (the phase log especially), split by replaying the edits in phase order across sequential branches rather than hunk-splitting one big diff.
- **Releases are tags on `main`**, which is the only trigger for `release.yml`. The full order — feature PR, release PR, tag, verify, AUR bump — is the **Release checklist** below; follow it rather than reconstructing it from memory.
- **Commit identity is the pseudonym only** — `Phundahl <89451493+Phundahl@users.noreply.github.com>`. Never let a real-name or employer-domain address into commit metadata; this is the attribution rule above, applied to Git itself.

## Privacy guard (enforced in CI)

The pseudonym rule above is enforced mechanically rather than left to review.
`scripts/privacy-check.sh` fails on:

1. absolute `/home/<user>` paths;
2. tailnet MagicDNS names that aren't obviously fictional (`example-tailnet`,
   `tailnet-demo`, `*.tailnet.ts.net` are allowlisted);
3. any email address other than the pseudonym's noreply one;
4. a commit **author** that isn't the pseudonym (the committer may also be
   GitHub's `noreply@github.com`, which is what a squash-merge stamps).

Two things to keep in mind when touching it. The checks are **structural, not a
denylist of real values** — a guard that named what it hides would leak it
itself, which is also why the script excludes itself from its own scan. And
check 4 covers **commit metadata**, which a scan of the working tree cannot see
at all: authorship is as public as file content and needs its own check.

**Where it runs, and why the order matters.** `scripts/githooks/{pre-commit,pre-push}`
run it locally (`--staged` / `--commits`), and the `privacy` CI job runs it again
as a backstop. **The local run is the one that prevents anything** — CI only sees
a branch once it is already on the remote, and GitHub retains pull-request refs
permanently, so a check that first runs in CI cannot stop something becoming
public. Activate the local hooks with
`git config core.hooksPath scripts/githooks` (a machine-wide `core.hooksPath`
that delegates here works too).

**Test fixtures come from `internal/tailscale/mock.go`'s fictional vocabulary,
never from live daemon output.** The mock package exists precisely so that
realistic-looking node names, users and addresses are available without reaching
for the real ones, and a structural guard can only catch the shapes it knows —
so the convention does the work the script can't.

## Release checklist (required)

Adopted after the v1.4.0 cycle, where the AUR package sat a version behind
because the step lived only in the maintainer's memory. **Do these in order** —
each step depends on the one before it landing on `main`.

1. **Feature PR** — the code, its tests, *and* its user-facing docs (README
   feature bullet, keybindings row, any caveat it creates) plus the phase-log
   entry. Docs ride with the feature, never with the release.
2. **Release PR** (`chore(release): vX.Y.Z`) — branched off `main` *after* step 1
   merges, or the changelog describes work the tag won't contain. Carries
   **only**: `appVersion` in `view.go` (the single definition), a new
   `## vX.Y.Z — YYYY-MM-DD` entry at the top of **`CHANGELOG.md`**, a refreshed
   "What's New" section in the README pointing at it, and the release
   phase-log entry. The README carries the **latest release only** — the full
   history lives in the changelog, so it stops growing.
3. **Tag `main`** — `git tag -a vX.Y.Z -m "…" && git push origin vX.Y.Z`.
   Annotated, not lightweight: goreleaser reads the tag object. Pushing the tag
   is the **only** trigger for `release.yml`. Confirm it points at the release
   commit before pushing — untagging after a release job has run is messy.
4. **Verify the published release** — don't trust the green tick alone:
   - `go version -m <binary>` shows the `-X main.version=` ldflag. Phase 29
     shipped a build where this silently did nothing, because Go ignores `-X`
     on a missing symbol; it is worth re-checking every time.
   - `sha256sum -c checksums.txt` on a downloaded artifact.
   - The `.deb` contains `/usr/share/tailtui/tailtui.toml.tpl` — see the
     `contrib/` packaging rule in Phase 31.
5. **Bump the AUR package** — a *separate* git repo
   (`aur@aur.archlinux.org:tailtui.git`), not automated, and invisible from
   this one, which is exactly why it gets forgotten. In that checkout:
   - `pkgver=X.Y.Z`, reset `pkgrel=1`, and replace `sha256sums` with
     `curl -sL https://github.com/Phundahl/tailtui/archive/vX.Y.Z.tar.gz | sha256sum`.
   - `makepkg --printsrcinfo > .SRCINFO` — the AUR rejects a push whose
     `.SRCINFO` disagrees with the PKGBUILD.
   - `makepkg -f` for a real build before pushing: it verifies the checksum,
     compiles, and produces the package. A broken PKGBUILD reaches users
     immediately, with no CI between you and them.
   - Commit as `upgpkg: tailtui X.Y.Z-1`, then push.
   - **To confirm the push landed, check the package page**
     (`https://aur.archlinux.org/packages/tailtui`) or cgit
     (`.../cgit/aur.git/plain/.SRCINFO?h=tailtui`) — both read the AUR's
     database directly. The **RPC endpoint (`/rpc/v5/info`) caches** and kept
     reporting the previous version well after the v1.4.0 push had succeeded,
     which looks exactly like a failed push if it is the only thing consulted.

The AUR PKGBUILD builds **from source** with Arch's hardening flags, and is
maintained by hand. goreleaser's `aurs:` block was considered and rejected: it
generates a `-bin` package that installs the prebuilt binary, which is not what
this package is, and automating it would mean either renaming the package or
dropping the source build. Revisit only if a `-bin` package is wanted on its
own merits.

## Documentation workflow (required)

At the end of **every** successful phase, automatically update both `CLAUDE.md` (technical decisions, architecture state, constraints) and `README.md` (tech stack, working features, run instructions). This is a standing user requirement, not a per-request ask.
